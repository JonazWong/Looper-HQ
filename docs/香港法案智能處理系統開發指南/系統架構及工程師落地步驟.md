[法院爬蟲]      [明報爬蟲]
       │              │
       ▼              ▼
   [檢索層] ← Embedding-3-Large
       │
       ▼
   [分類層] ← Legal-BERT / LexNLP
       │
       ▼
   [語言層] ← GPT-4 Turbo
       │
       ▼
   [輸出層] → 中英文結果 + 分類標籤 + 摘要

工程師落地步驟
建立爬蟲 → 存入 documents

跑 Embedding → 存入 embeddings

NLP 分類 → 存入 classifications

GPT-4 Turbo 翻譯與摘要 → 存入 translations 與 summaries

前端查詢 → 呼叫 API → 顯示結果
