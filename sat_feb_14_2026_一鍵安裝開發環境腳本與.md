# Copilot Chat Conversation Export: 一鍵安裝開發環境腳本與

**User:** @JonazWong
**Thread URL:** https://github.com/copilot/c/5d375d73-2cf6-4243-921d-504fc119ea07

## @JonazWong

專案問題已修正。由於專案是屬於大型及多方面，本機的代理單靠文件未必能完整正確安裝。請幫忙制作一鍵自動安裝(有回復功能更好)，不知道需不需要分幾大類安裝，比如搜尋爬蟲的一類，管理數據庫一類.最後有沒有一鍵自動生成將所有頁面自動生成,並設置好關聯(如有)及生成安裝架構指南

## Copilot

下面先用中文幫你把需求整理清楚，然後給一個你可以在專案裡實作的「一鍵安裝與恢復」設計方案。因為我目前還沒讀到你 repo 裡的實際檔案內容，只能先給結構化設計與範本腳本；等你願意的時候可以提供 repo 連結或主要檔案清單，我再幫你具體填滿細節指令。

---

## 一、先確認你的目標

你希望在 `Looper-HQ` 專案裡有一套：

1. **一鍵安裝**：  
   - 自動檢查環境  
   - 按「功能子系統」分階段安裝（例如：搜尋爬蟲、資料庫管理、後端 / 前端等）  
   - 自動產生必要設定檔

2. **回復 / 還原功能（Rollback / Uninstall）**：  
   - 安裝前自動建立快照（例如 `.env` / config 備份、資料庫 schema snapshot）  
   - 出錯時可以「回復上一次正常狀態」

3. **一鍵生成頁面與關聯**（我理解為：初始化系統必要資料／頁面結構）  
   - 例如：預設管理後台帳號、預設目錄／菜單、預設範例頁面、權限關聯
   - 並輸出一份「安裝架構指南」給使用者（Markdown 檔）

---

## 二、整體建議：用一個 `scripts/` 目錄管理

建議在專案根目錄新增：

- `scripts/install.sh`：主安裝入口（Linux / macOS 用）
- `scripts/install.ps1`：Windows PowerShell 版本（如果你需要支援 Windows）
- `scripts/uninstall.sh`：解除安裝 / 還原
- `scripts/bootstrap_data.py` 或 `bootstrap_data.ts` / `bootstrap_data.js`：在資料庫中「一鍵建立頁面與關聯」的程式
- `docs/install-guide.md`：自動生成或半自動生成的安裝架構指南

下面以 **Linux/macOS + Shell +（假設）後端用 Node.js + 資料庫用 PostgreSQL** 來舉例，你可以依實際技術棧替換（Python、MySQL 等）。

---

## 三、一鍵安裝腳本設計（`scripts/install.sh`）

以下是可直接放進 repo 的範本，請依專案實際情況調整命令（例如 `npm install` 改成 `pip install -r requirements.txt` 等）。

```bash name=scripts/install.sh
#!/usr/bin/env bash
set -euo pipefail

# ========= 基本設定 =========
PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
BACKUP_DIR="${PROJECT_ROOT}/.install_backups"
LOG_FILE="${PROJECT_ROOT}/install.log"

mkdir -p "${BACKUP_DIR}"

log() {
  echo "[$(date '+%Y-%m-%d %H:%M:%S')] $*" | tee -a "${LOG_FILE}"
}

abort() {
  log "安裝失敗：$*"
  log "如需回復，請執行：scripts/uninstall.sh --rollback-latest"
  exit 1
}

# ========= 快照與備份 =========
create_snapshot() {
  SNAPSHOT_ID="$(date '+%Y%m%d-%H%M%S')"
  SNAPSHOT_PATH="${BACKUP_DIR}/${SNAPSHOT_ID}"
  mkdir -p "${SNAPSHOT_PATH}"

  log "建立安裝前快照：${SNAPSHOT_ID}"

  # 1. 備份環境變數與配置
  if [ -f "${PROJECT_ROOT}/.env" ]; then
    cp "${PROJECT_ROOT}/.env" "${SNAPSHOT_PATH}/.env.bak"
  fi

  if [ -d "${PROJECT_ROOT}/config" ]; then
    tar czf "${SNAPSHOT_PATH}/config.tar.gz" -C "${PROJECT_ROOT}" config
  fi

  # 2. 資料庫 Schema 快照（依實際 DB 調整）
  # 例：PostgreSQL
  if command -v pg_dump >/dev/null 2>&1; then
    if [ -n "${DB_NAME:-}" ] && [ -n "${DB_USER:-}" ]; then
      log "備份資料庫結構（只 schema，不含資料）"
      pg_dump --schema-only -U "${DB_USER}" "${DB_NAME}" > "${SNAPSHOT_PATH}/db_schema.sql" || \
        log "警告：資料庫 schema 備份失敗（可忽略，視情況調整）"
    fi
  fi

  echo "${SNAPSHOT_ID}" > "${BACKUP_DIR}/latest"
}

# ========= 環境檢查 =========
check_prerequisites() {
  log "檢查系統環境..."

  # 例：Node.js + npm
  if ! command -v node >/dev/null 2>&1; then
    abort "未找到 node，請先安裝 Node.js"
  fi
  if ! command -v npm >/dev/null 2>&1; then
    abort "未找到 npm，請先安裝 npm"
  fi

  # 例：Git
  if ! command -v git >/dev/null 2>&1; then
    abort "未找到 git，請先安裝 Git"
  fi

  # 例：PostgreSQL client
  if ! command -v psql >/dev/null 2>&1; then
    log "警告：未找到 psql，部分資料庫相關操作可能無法完成"
  fi

  log "環境檢查完成。"
}

# ========= 分模組安裝 =========

install_search_crawler() {
  log "開始安裝：搜尋爬蟲模組"

  # 依專案實際路徑調整，例如 services/crawler
  local CRAWLER_DIR="${PROJECT_ROOT}/services/crawler"
  if [ -d "${CRAWLER_DIR}" ]; then
    pushd "${CRAWLER_DIR}" >/dev/null

    # 例：Node.js crawler
    if [ -f package.json ]; then
      npm install
    fi

    # 例：Python crawler
    if [ -f requirements.txt ]; then
      pip install -r requirements.txt
    fi

    popd >/dev/null
  else
    log "略過：未找到搜尋爬蟲模組目錄 ${CRAWLER_DIR}"
  fi

  log "完成：搜尋爬蟲模組安裝"
}

install_database_layer() {
  log "開始安裝：資料庫與管理模組"

  # 設定資料庫（依專案調整）
  # 1. 建立資料庫（如需要）
  if command -v createdb >/dev/null 2>&1 && [ -n "${DB_NAME:-}" ]; then
    createdb "${DB_NAME}" 2>/dev/null || log "資料庫 ${DB_NAME} 已存在，略過建立"
  fi

  # 2. 執行 migration（依你的 migration 工具調整，如 Prisma、Knex、Alembic 等）
  local API_DIR="${PROJECT_ROOT}/services/api"
  if [ -d "${API_DIR}" ]; then
    pushd "${API_DIR}" >/dev/null

    if [ -f package.json ]; then
      npm install
      if npm run | grep -q "migrate"; then
        npm run migrate || abort "資料庫 migration 失敗"
      fi
    fi

    popd >/dev/null
  fi

  log "完成：資料庫與管理模組安裝"
}

install_backend_frontend() {
  log "開始安裝：後端 / 前端"

  # 1. 後端
  local API_DIR="${PROJECT_ROOT}/services/api"
  if [ -d "${API_DIR}" ]; then
    pushd "${API_DIR}" >/dev/null
    if [ -f package.json ]; then
      npm install
      # 可選：npm run build
    fi
    popd >/dev/null
  fi

  # 2. 前端
  local WEB_DIR="${PROJECT_ROOT}/services/web"
  if [ -d "${WEB_DIR}" ]; then
    pushd "${WEB_DIR}" >/dev/null
    if [ -f package.json ]; then
      npm install
      # 可選：npm run build
    fi
    popd >/dev/null
  fi

  log "完成：後端 / 前端安裝"
}

bootstrap_pages_and_relations() {
  log "開始初始化：預設頁面、關聯與基礎資料"

  # 建議用一支專門腳本處理，語言依你專案而定
  # 例如：node scripts/bootstrap_data.js 或 python scripts/bootstrap_data.py
  if [ -f "${PROJECT_ROOT}/scripts/bootstrap_data.js" ]; then
    node "${PROJECT_ROOT}/scripts/bootstrap_data.js" || abort "初始化頁面與關聯失敗"
  elif [ -f "${PROJECT_ROOT}/scripts/bootstrap_data.py" ]; then
    python "${PROJECT_ROOT}/scripts/bootstrap_data.py" || abort "初始化頁面與關聯失敗"
  else
    log "略過：未找到 bootstrap_data 腳本，請日後補上"
  fi

  log "完成：初始化預設頁面與關聯"
}

generate_install_guide() {
  log "生成安裝架構指南..."

  local DOC_DIR="${PROJECT_ROOT}/docs"
  mkdir -p "${DOC_DIR}"

  cat > "${DOC_DIR}/install-guide.md" << 'EOF'
# Looper-HQ 安裝架構指南

> 本檔案由 `scripts/install.sh` 自動生成，可視需要手動補充與修改。

## 1. 專案模組分層

- 搜尋爬蟲模組：`services/crawler`
- 後端 API：`services/api`
- 前端 Web：`services/web`
- 公用腳本：`scripts/`

## 2. 安裝步驟概覽

1. 環境檢查（Node.js、Git、資料庫客戶端等）
2. 建立安裝前快照（.env、config、資料庫 schema）
3. 安裝搜尋爬蟲模組依賴
4. 安裝資料庫與管理模組並執行 migration
5. 安裝後端 / 前端依賴
6. 初始化預設頁面與關聯資料

## 3. 一鍵安裝指令

```bash
cd /path/to/Looper-HQ
chmod +x scripts/install.sh
./scripts/install.sh
```

## 4. 回復 / 還原

```bash
# 回到最近一次安裝前快照
./scripts/uninstall.sh --rollback-latest
```

## 5. 頁面與關聯初始化

安裝腳本會調用 `scripts/bootstrap_data.*` ，
自動建立：

- 預設管理者帳號
- 預設頁面 / 選單
- 預設權限與關聯

請依系統實際需要在 `scripts/bootstrap_data.*` 中補上邏輯。

EOF

  log "安裝架構指南已生成：docs/install-guide.md"
}

# ========= 主流程 =========

main() {
  log "===== Looper-HQ 一鍵安裝開始 ====="

  check_prerequisites
  create_snapshot

  install_search_crawler
  install_database_layer
  install_backend_frontend
  bootstrap_pages_and_relations
  generate_install_guide

  log "===== Looper-HQ 一鍵安裝完成 ====="
  log "詳細紀錄：${LOG_FILE}"
}

main "$@"
```

使用前記得：

```bash
chmod +x scripts/install.sh
```

---

## 四、回復 / 還原腳本（`scripts/uninstall.sh`）

這支腳本的重點是：

- 從 `.install_backups/latest` 找到上一次安裝前的快照 ID
- 還原 `.env` / config
- 視需要還原資料庫 schema（可做成選項：只還原 schema、或全刪 DB 重建）

```bash name=scripts/uninstall.sh
#!/usr/bin/env bash
set -euo pipefail

PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
BACKUP_DIR="${PROJECT_ROOT}/.install_backups"
LOG_FILE="${PROJECT_ROOT}/uninstall.log"

log() {
  echo "[$(date '+%Y-%m-%d %H:%M:%S')] $*" | tee -a "${LOG_FILE}"
}

abort() {
  log "操作失敗：$*"
  exit 1
}

usage() {
  cat << EOF
用法：
  scripts/uninstall.sh --rollback-latest    回復到最近一次安裝前快照
  scripts/uninstall.sh --rollback <ID>      回復到指定快照 ID
EOF
  exit 1
}

get_snapshot_id() {
  local MODE="${1:-}"
  local ID="${2:-}"

  if [ "${MODE}" = "--rollback-latest" ]; then
    if [ ! -f "${BACKUP_DIR}/latest" ]; then
      abort "找不到 latest 快照資訊"
    fi
    cat "${BACKUP_DIR}/latest"
  elif [ "${MODE}" = "--rollback" ]; then
    if [ -z "${ID}" ]; then
      abort "請提供快照 ID"
    fi
    echo "${ID}"
  else
    usage
  fi
}

rollback() {
  local SNAPSHOT_ID="$1"
  local SNAPSHOT_PATH="${BACKUP_DIR}/${SNAPSHOT_ID}"

  if [ ! -d "${SNAPSHOT_PATH}" ]; then
    abort "快照目錄不存在：${SNAPSHOT_PATH}"
  fi

  log "開始回復快照：${SNAPSHOT_ID}"

  # 1. 還原 .env
  if [ -f "${SNAPSHOT_PATH}/.env.bak" ]; then
    cp "${SNAPSHOT_PATH}/.env.bak" "${PROJECT_ROOT}/.env"
    log "已還原 .env"
  fi

  # 2. 還原 config
  if [ -f "${SNAPSHOT_PATH}/config.tar.gz" ]; then
    tar xzf "${SNAPSHOT_PATH}/config.tar.gz" -C "${PROJECT_ROOT}"
    log "已還原 config 目錄"
  fi

  # 3. 視需要還原資料庫 schema（此處僅示範，可依實際情況加上互動提示）
  if [ -f "${SNAPSHOT_PATH}/db_schema.sql" ] && command -v psql >/dev/null 2>&1; then
    if [ -n "${DB_NAME:-}" ] && [ -n "${DB_USER:-}" ]; then
      log "準備還原資料庫 schema 至 ${DB_NAME}（僅結構）"
      psql -U "${DB_USER}" "${DB_NAME}" < "${SNAPSHOT_PATH}/db_schema.sql" || \
        log "警告：資料庫 schema 還原失敗，請手動檢查"
    fi
  fi

  log "回復完成：${SNAPSHOT_ID}"
}

main() {
  if [ "$#" -eq 0 ]; then
    usage
  fi

  local MODE="$1"
  local ID="${2:-}"

  local SNAPSHOT_ID
  SNAPSHOT_ID="$(get_snapshot_id "${MODE}" "${ID}")"

  rollback "${SNAPSHOT_ID}"
}

main "$@"
```

同樣記得：

```bash
chmod +x scripts/uninstall.sh
```

---

## 五、一鍵生成頁面與關聯（`scripts/bootstrap_data.*`）

這部分必須依你實際的技術棧與資料庫 schema 來寫。大致流程：

1. 連線到資料庫
2. 建立：
   - 預設角色（例如：admin、editor、viewer）
   - 預設使用者（例如：`admin@looper.local`，密碼先要求安裝後修改）
   - 預設頁面／模組（例如：Dashboard、Crawler 管理、資料庫管理）
   - 預設菜單結構＋頁面關聯
   - 預設權限與角色關聯
3. 避免重複建立（先 `SELECT` 判斷是否已存在）

假設你後端是 Node.js + TypeScript + TypeORM（只作為示例）：

```typescript name=scripts/bootstrap_data.ts
import "reflect-metadata";
import { DataSource } from "typeorm";
import { User } from "../services/api/src/entities/User";
import { Role } from "../services/api/src/entities/Role";
import { Page } from "../services/api/src/entities/Page";
import { Menu } from "../services/api/src/entities/Menu";

const AppDataSource = new DataSource({
  type: "postgres",
  url: process.env.DATABASE_URL,
  entities: [User, Role, Page, Menu],
});

async function bootstrap() {
  await AppDataSource.initialize();

  const roleRepo = AppDataSource.getRepository(Role);
  const userRepo = AppDataSource.getRepository(User);
  const pageRepo = AppDataSource.getRepository(Page);
  const menuRepo = AppDataSource.getRepository(Menu);

  // 1. 角色
  let adminRole = await roleRepo.findOne({ where: { name: "admin" } });
  if (!adminRole) {
    adminRole = roleRepo.create({ name: "admin", description: "系統管理員" });
    await roleRepo.save(adminRole);
  }

  // 2. 管理員帳號
  let adminUser = await userRepo.findOne({ where: { email: "admin@looper.local" } });
  if (!adminUser) {
    adminUser = userRepo.create({
      email: "admin@looper.local",
      // TODO: 替換為你的 hash 密碼邏輯
      passwordHash: "CHANGE_ME",
      roles: [adminRole],
    });
    await userRepo.save(adminUser);
  }

  // 3. 頁面
  const pagesToEnsure = [
    { code: "dashboard", name: "總覽儀表板" },
    { code: "crawler", name: "搜尋爬蟲管理" },
    { code: "db_admin", name: "資料庫管理" },
  ];

  for (const p of pagesToEnsure) {
    let page = await pageRepo.findOne({ where: { code: p.code } });
    if (!page) {
      page = pageRepo.create(p);
      await pageRepo.save(page);
    }
  }

  // 4. 菜單與關聯（視你的 schema 而定，這裡略）

  await AppDataSource.destroy();
  console.log("Bootstrap 完成：預設角色、使用者、頁面已建立（若原本不存在）。");
}

bootstrap().catch((err) => {
  console.error("Bootstrap 失敗", err);
  process.exit(1);
});
```

安裝腳本就能呼叫：

```bash
node scripts/bootstrap_data.js
# 或 ts-node scripts/bootstrap_data.ts
```

---

## 六、下一步我可以幫你做什麼？

如果你願意：

1. 貼出 `Looper-HQ` 的主要目錄結構（`ls` 結果或 README 關鍵部分），或  
2. 告訴我：
   - 後端語言 / 框架（Node / Python / Laravel / …）
   - 前端是否獨立（React / Vue / 無）
   - 資料庫種類（Postgres / MySQL / MongoDB / 其他）

我可以針對你實際的技術棧：

- 把 `install.sh` 裡目前「範例命令」全部替換成你專案真實需要的命令
- 幫你設計更精準的「搜尋爬蟲 / 資料庫 / 其他模組」分組與安裝順序
- 幫你寫好 `bootstrap_data` 的具體 SQL / ORM 實作草稿

你可以先把上面的 `scripts/` 與 `docs/` 加進 repo，跑跑看，再告訴我出現的錯誤訊息，我可以陪你一輪一輪修到能真正「一鍵裝完＋可回復」為止。

## @JonazWong

這兩份可以嗎？

## Copilot

兩份內容本身都「有用也正確」，但如果目標是：

1. 讓不熟悉技術的人也能「一鍵安裝、可恢復」  
2. 清楚區分：開發環境安裝 / 正式部署 / 各子系統（爬蟲、DB、Web）安裝  
3. 透過腳本自動化盡量多的步驟  

那目前還「不夠」，需要再補一層「自動化腳本 + 統一安裝說明」。

我分成三點回答你：

---

## 1. 這兩份文件各自做得不錯的地方

### README.md（專案根）

優點：

- 已經有一個很清楚的「本機開發安裝流程」：

```bash
git clone https://github.com/JonazWong/Looper-HQ.git
cd Looper-HQ
pnpm install
cp .env.example .env
pnpm docker:up
pnpm db:push
pnpm db:seed
pnpm dev:all
```

- 有把「兩個 app」分開說明（`dev:web` / `dev:legal`）
- 也有把「爬蟲相關指令」整理好（`pnpm crawler:*`）
- 有 CI/CD / Health / 自動修復 的描述，對維運很有幫助

結論：  
**作為開發者的總覽與手動安裝指南，是足夠的。**

---

### DEPLOYMENT.md（部署指南）

優點：

- 對 DigitalOcean App Platform 的流程寫得非常完整：
  - 準備 `.env.production`
  - 建 PostgreSQL
  - 設 domain、HTTPS
  - 執行 Prisma migrate
- 有安全檢查清單、成本估算、常見錯誤排查，對「實際上線」很實用

結論：  
**作為「雲端正式部署」文件，很完整。**

---

## 2. 為什麼還不算「一鍵安裝 / 可恢復 / 子系統分模組安裝」？

你原本的需求是：

1. 專案大、模組多，「本機代理」只看文件未必能正確安裝  
2. 想要「一鍵自動安裝」（最好有恢復功能）  
3. 可能需要區分幾大類安裝（例如：搜尋爬蟲一類、資料庫管理一類）  
4. 最後希望有「一鍵自動生成所有頁面 / 關聯（如有）」＋「安裝架構指南」

目前 README + DEPLOYMENT **還缺這幾點**：

1. **缺「一鍵腳本」**  
   - 現在是「使用者自己一行一行輸入指令」  
   - 沒有 `scripts/install.sh` 或 `pnpm setup` 這種「全部幫你跑完」的入口  
   - 也沒有 `rollback / uninstall` 的還原腳本

2. **缺「依子系統分模組安裝的自動流程」**  
   雖然指令已經有分類：
   - Docker → `pnpm docker:up`  
   - Database → `pnpm db:push / db:seed`  
   - Crawler → `pnpm crawler:*`  
   但全部還是手動執行，沒有一個像：
   ```bash
   pnpm setup:search
   pnpm setup:database
   pnpm setup:web
   ```
   這種「子系統級」的安裝命令或腳本。

3. **缺「自動初始化系統資料／頁面／關聯」的腳本**  
   README 有說 public cases、crawler、/public-cases 等，但：
   - 沒有一支 `scripts/bootstrap_data.ts`（或類似）自動建立：
     - 預設租戶 / 帳號
     - 預設頁面 / menu 結構
     - 權限與角色關聯
   - 也沒有一個「一鍵生成安裝架構指南」的腳本，現在的 DEPLOYMENT.md 是人工寫好的，不是由腳本隨安裝狀態更新。

4. **缺「可恢復（Rollback）」的設計**  
   - 沒有在安裝前自動備份 `.env` / config / schema  
   - 也沒有 `scripts/uninstall.sh` 這種可還原到「上一次乾淨狀態」的工具

---

## 3. 建議你下一步這樣做（我可以幫你寫）

這兩份文件可以視為「基底說明」，在這之上我們再加：

### A. 新增一個總安裝腳本（本機／開發環境）

例如在專案根目錄增加：

- `scripts/install-dev.sh`：一鍵本機安裝（我前一則訊息已給你一個可直接用的範本）
  - 內部自動做：
    - 檢查：Node + pnpm + Docker + PostgreSQL client
    - 建立安裝快照（備份 `.env`、config、DB schema）
    - `pnpm install`
    - `cp .env.example .env`（如不存在）
    - `pnpm docker:up`
    - `pnpm db:push`
    - `pnpm db:seed`
    - 必要時呼叫 `pnpm crawler:all` 做初始資料
    - 呼叫 `node/tsx scripts/bootstrap_data.ts` 初始化頁面 & 關聯
    - 自動產生一份 `docs/install-guide.md`（說明剛剛幫你做了什麼）

這樣：

```bash
git clone https://github.com/JonazWong/Looper-HQ.git
cd Looper-HQ
chmod +x scripts/install-dev.sh
./scripts/install-dev.sh
```

就能幾乎「零操作」裝好開發環境。

### B. 新增子系統安裝指令（對映你說的「幾大類」）

在 `package.json`（或 `pnpm-workspace` root）加上 scripts，例如：

```jsonc
{
  "scripts": {
    "setup:base": "pnpm install && pnpm docker:up && pnpm db:push && pnpm db:seed",
    "setup:web": "pnpm dev:web",
    "setup:legal": "pnpm dev:legal",
    "setup:crawler": "pnpm crawler:all",
    "setup:database": "pnpm db:push && pnpm db:seed",
    "setup:all": "pnpm run setup:base && pnpm run setup:crawler"
  }
}
```

搭配 `scripts/install-dev.sh`，使用者可以：

```bash
pnpm setup:database
pnpm setup:crawler
pnpm setup:all
```

這樣就明確把你想要的「搜尋爬蟲一類、管理資料庫一類」拆開。

### C. 新增恢復腳本

如我之前給你的 `scripts/uninstall.sh` 範本：

- 在安裝時寫入 `.install_backups/<timestamp>/`：
  - `.env.bak`
  - `config.tar.gz`
  - `db_schema.sql`
- `uninstall.sh --rollback-latest` → 回到上一個快照

### D. 新增「初始化頁面／關聯」腳本

依你實際 schema（Prisma + PostgreSQL）來寫一個：

- `scripts/bootstrap_data.ts`（用 `tsx` 或 `ts-node` 執行）
  - 使用 `@looper-hq/database` 的 Prisma Client
  - 建立：
    - 預設 tenant / firm
    - admin user（例如 email: `admin@local.looper`，提醒部署後要改密碼）
    - 預設頁面（Dashboard、Case Search、Public Cases 等）
    - 導航結構（sidebar menu）與 route 關聯
    - 預設角色 / 權限關聯（`ADMIN | LAWYER | STAFF | CLIENT`）

然後在 README 補一句：

```bash
# 初始化系統資料（可選，只需執行一次）
pnpm tsx scripts/bootstrap_data.ts
```

或在 `install-dev.sh` 自動幫你跑。

### E. 統一把「自動化流程」寫到一份新的指南

目前有：

- README.md（總覽 + 快速上手）
- docs/QUICKSTART.md
- DEPLOYMENT.md（你貼的）

建議再加一份（或改寫一份）：

- `docs/INSTALL_AUTOMATION.md`（或 `docs/install-guide.md`）  
  裡面專門寫：

  - 「給一般工程師 / 代理，只要這樣執行就好」：
    ```bash
    ./scripts/install-dev.sh
    ```
  - 想要分模組安裝怎麼做：
    ```bash
    pnpm setup:database
    pnpm setup:crawler
    pnpm setup:web
    ```
  - 出問題怎樣回復：
    ```bash
    ./scripts/uninstall.sh --rollback-latest
    ```

---

## 總結回答你的問題

> 這兩份可以嗎？

- **如果問題是：「這兩份當作人工閱讀的安裝／部署文件，是否 OK？」→ 可以，而且寫得不錯。**
- **如果問題是：「有了這兩份，就等於完成你說的『一鍵自動安裝（含回復）＋分類安裝＋自動生成頁面與安裝架構指南』嗎？」→ 還不夠，還差：**
  - 自動化腳本（install / uninstall / bootstrap）
  - 子系統分模組安裝指令
  - 利用腳本生成或更新的「安裝架構指南」

---

如果你願意下一步，我可以直接幫你：

1. 草擬 `scripts/install-dev.sh`，專門針對 Looper-HQ 目前的 `pnpm` 指令（不是通用範本，而是用你 README 內的實際命令改寫）
2. 草擬 `scripts/bootstrap_data.ts`，用 Prisma（照你的 monorepo 架構，放在正確位置）
3. 給一版 `package.json` 的 `scripts` 修改方案，讓「搜尋爬蟲 / 資料庫 / Web」各有一組 `setup:*` 指令

你只要回覆一句：「幫我用目前 README 裡的指令寫成真正的 install-dev.sh & package.json scripts」，我就會用你的 repo 結構來具體寫出來。

## @JonazWong

「幫我用目前 README 裡的指令寫成真正的 install-dev.sh & package.json scripts

## Copilot

下面先直接給你可以放進專案裡用的兩個東西：

1. `scripts/install-dev.sh`：一鍵本機開發環境安裝（含簡單檢查與 .env 處理）  
2. 根目錄 `package.json` 的 `scripts` 區段建議內容：把 README 裡零散指令整理成分類好的 `setup:*` 指令

你可以先照貼，之後如果 repo 裡已經有同名 script，我們再幫你 merge。

---

## 1. 一鍵安裝腳本：`scripts/install-dev.sh`

說明：

- 用 README 的流程做自動化：

  1. `pnpm install`  
  2. 如果沒有 `.env`，就從 `.env.example` 複製  
  3. `pnpm docker:up`  
  4. `pnpm db:push`  
  5. `pnpm db:seed`  
  6. 最後提示你可以 `pnpm dev:all` 開兩個 app

- 加了基本防呆：
  - 檢查 `pnpm`、`docker` 是否存在
  - 在 `install.log` 紀錄過程
- 目前先不做 rollback / snapshot，如果你覺得這版好用，我再幫你升級成有快照功能的版本。

```bash name=scripts/install-dev.sh
#!/usr/bin/env bash
set -euo pipefail

PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
LOG_FILE="${PROJECT_ROOT}/install.log"

log() {
  echo "[$(date '+%Y-%m-%d %H:%M:%S')] $*" | tee -a "${LOG_FILE}"
}

abort() {
  log "安裝失敗：$*"
  exit 1
}

check_prerequisites() {
  log "檢查系統環境..."

  if ! command -v pnpm >/dev/null 2>&1; then
    abort "未找到 pnpm，請先安裝 pnpm（https://pnpm.io/installation）"
  fi

  if ! command -v docker >/dev/null 2>&1; then
    abort "未找到 docker，請先安裝 Docker Desktop / Docker Engine"
  fi

  log "環境檢查完成。"
}

prepare_env_file() {
  cd "${PROJECT_ROOT}"

  if [ -f ".env" ]; then
    log ".env 已存在，略過從 .env.example 複製"
  else
    if [ -f ".env.example" ]; then
      log "未找到 .env，從 .env.example 自動建立..."
      cp .env.example .env
    else
      log "警告：找不到 .env 與 .env.example，請手動建立環境變數檔案"
    fi
  fi
}

install_dependencies() {
  cd "${PROJECT_ROOT}"
  log "執行 pnpm install..."
  pnpm install
}

start_docker_services() {
  cd "${PROJECT_ROOT}"
  log "啟動 Docker 服務（pnpm docker:up）..."
  pnpm docker:up
}

migrate_database() {
  cd "${PROJECT_ROOT}"
  log "同步資料庫 schema（pnpm db:push）..."
  pnpm db:push
}

seed_database() {
  cd "${PROJECT_ROOT}"
  log "寫入測試資料（pnpm db:seed）..."
  pnpm db:seed
}

main() {
  log "===== Looper-HQ 本機開發環境一鍵安裝開始 ====="

  check_prerequisites
  install_dependencies
  prepare_env_file
  start_docker_services
  migrate_database
  seed_database

  log "===== 安裝流程完成 ====="
  log "接下來你可以執行："
  log "  pnpm dev:all    # 同時啟動 web + legal-case-search"
  log "或個別啟動："
  log "  pnpm dev:web    # http://localhost:3000"
  log "  pnpm dev:legal  # http://localhost:3001"
}

main "$@"
```

使用方式（專案根目錄）：

```bash
chmod +x scripts/install-dev.sh
./scripts/install-dev.sh
```

---

## 2. 整理後的 `package.json` scripts（根目錄）

下面是一個「只動 scripts 區段」的設計，把你 README 中所有腳本整理成幾大類：

- 開發：`dev:*`
- 資料庫：`db:*`
- Docker：`docker:*`
- 爬蟲：`crawler:*`
- 安裝 / 設定：`setup:*`

如果你根目錄已經有 `package.json`，請只合併 `scripts` 這塊；如果還沒有，這段可以直接當作 `package.json` 的一部分（記得補上 `name`、`version` 等欄位）。

```jsonc name=package.json
{
  // ... 其他欄位（name, version, dependencies 等）

  "scripts": {
    // === 一鍵開發環境安裝 ===
    "setup:dev": "bash ./scripts/install-dev.sh",

    // === 開發環境啟動 ===
    "dev": "pnpm dev:all",
    "dev:all": "turbo run dev --parallel --filter=web --filter=legal-case-search",
    "dev:web": "turbo run dev --filter=web",
    "dev:legal": "turbo run dev --filter=legal-case-search",

    // === 資料庫相關 ===
    "db:migrate": "pnpm --filter @looper-hq/database prisma migrate deploy",
    "db:push": "pnpm --filter @looper-hq/database prisma db push",
    "db:seed": "pnpm --filter @looper-hq/database prisma db seed",
    "db:studio": "pnpm --filter @looper-hq/database prisma studio",

    // === Docker 相關 ===
    "docker:up": "docker compose -f ./infrastructure/docker/docker-compose.yml up -d",
    "docker:down": "docker compose -f ./infrastructure/docker/docker-compose.yml down",

    // === Crawlers（依 README） ===
    "crawler:all": "pnpm --filter legal-case-search run crawler:all",
    "crawler:judiciary": "pnpm --filter legal-case-search run crawler:judiciary",
    "crawler:rss": "pnpm --filter legal-case-search run crawler:rss",

    // === Build & Test ===
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  }

  // ... 其他欄位
}
```

說明與假設：

- `dev:*`：  
  - 我假設你使用 Turborepo（README 有 `packages/` / `apps/` 結構），所以用 `turbo run` 來同時啟動多 app。  
  - 如果你目前是直接在 root 寫 `pnpm dev:web` 等，就把 `dev:all` 等命令改回你現有的內容即可。

- `db:*`：  
  - README 提到「Shared Prisma database package」，我假設它的 package 名稱是 `@looper-hq/database`。  
  - 如果實際名稱不同（例如 `@looper/database`），請把 `--filter @looper-hq/database` 改成真實名稱。  
  - 若你目前是在 `apps/web` 裡跑 `prisma`，也可以直接改成：
    ```jsonc
    "db:push": "cd apps/web && pnpm prisma db push",
    "db:seed": "cd apps/web && pnpm prisma db seed",
    "db:studio": "cd apps/web && pnpm prisma studio"
    ```

- `docker:*`：  
  - 根據 README 的 `infrastructure/docker/` 結構，我假設有 `docker-compose.yml`；如果檔名或路徑不同，請把它改掉。  
  - 若你原本在 root 已經有對應的 `docker:up` / `docker:down`，可以保留你原本的內容，只是 scripts 名稱保持一致。

- `crawler:*`：  
  - README 顯示是 `pnpm crawler:*`，但沒有指到哪個 app：我假設 crawler 是掛在 `apps/legal-case-search` 的 scripts 裡，所以從 root 用 `--filter legal-case-search` 呼叫。  
  - 如果你其實是在 root 直接定義 `crawler:all`，就把這三個改回你現在的寫法即可：
    ```jsonc
    "crawler:all": "ts-node scripts/crawlers/run-all.ts",
    // 之類的
    ```

- `setup:dev`：  
  - 這就是一個對映你剛剛要的「一鍵安裝」命令：
    ```bash
    pnpm setup:dev
    ```
  - 底層就是執行 `scripts/install-dev.sh`。

---

## 3. 建議你下一步這樣驗證

1. 在 repo 新增檔案：

   - `scripts/install-dev.sh`（貼上上面的內容）  
   - 如果沒有 `scripts` 目錄，先建一個 `scripts/`

2. 給執行權限（macOS / Linux）：

   ```bash
   cd Looper-HQ
   chmod +x scripts/install-dev.sh
   ```

3. 修改 root `package.json` 的 `scripts` 區段（先備份原來的，免得衝突）  

4. 測試一鍵安裝：

   ```bash
   pnpm setup:dev
   ```

   看看是否順利跑完 `install → env → docker:up → db:push → db:seed`。

---

如果你願意把目前根目錄的 `package.json` 內容貼給我（或至少 `scripts` 那一段），我可以幫你：

- 直接「對照你現有的實際 script 名稱」調整成完全 match 的版本  
- 把 `crawler:*` / `db:*` 的 filter 路徑改成和你 monorepo 真實配置一致  

這樣可以做到：你只需要跑 `pnpm setup:dev`，其他讓腳本和 scripts 自己處理。

## @JonazWong

請替我安排

## Copilot

下面是幫你「整理好且直接可用」的兩部分：

1. `scripts/install-dev.sh`：一鍵本機安裝腳本（用你現在 `package.json` 的指令）  
2. 更新後的 `package.json`：在原有 scripts 基礎上，加上 `setup:*` 等不破壞舊流程的指令

---

## 一、`scripts/install-dev.sh`（一鍵開發環境安裝）

這支腳本完全依照你目前 `package.json` 的指令來排流程：

- `pnpm install`
- `.env` 不存在時自動從 `.env.example` 建立
- `pnpm docker:up`
- `pnpm db:push`
- `pnpm db:seed`
- 最後提示怎麼啟動 `dev:web / dev:legal / dev:all`

請在專案根目錄建立 `scripts/install-dev.sh`，內容如下：

```bash name=scripts/install-dev.sh
#!/usr/bin/env bash
set -euo pipefail

PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
LOG_FILE="${PROJECT_ROOT}/install-dev.log"

log() {
  echo "[$(date '+%Y-%m-%d %H:%M:%S')] $*" | tee -a "${LOG_FILE}"
}

abort() {
  log "安裝失敗：$*"
  exit 1
}

check_prerequisites() {
  log "檢查系統環境..."

  if ! command -v pnpm >/dev/null 2>&1; then
    abort "未找到 pnpm，請先安裝 pnpm（https://pnpm.io/installation）"
  fi

  if ! command -v docker >/dev/null 2>&1; then
    abort "未找到 docker，請先安裝 Docker Desktop / Docker Engine"
  fi

  log "環境檢查完成。"
}

prepare_env_file() {
  cd "${PROJECT_ROOT}"

  if [ -f ".env" ]; then
    log ".env 已存在，略過從 .env.example 複製"
  else
    if [ -f ".env.example" ]; then
      log "未找到 .env，從 .env.example 自動建立..."
      cp .env.example .env
    else
      log "警告：找不到 .env 與 .env.example，請手動建立環境變數檔案"
    fi
  fi
}

install_dependencies() {
  cd "${PROJECT_ROOT}"
  log "執行 pnpm install..."
  pnpm install
}

start_docker_services() {
  cd "${PROJECT_ROOT}"
  log "啟動 Docker 服務（pnpm docker:up）..."
  pnpm docker:up
}

migrate_database() {
  cd "${PROJECT_ROOT}"
  log "同步資料庫 schema（pnpm db:push）..."
  pnpm db:push
}

seed_database() {
  cd "${PROJECT_ROOT}"
  log "寫入種子資料（pnpm db:seed）..."
  pnpm db:seed
}

main() {
  log "===== Looper-HQ 本機開發環境一鍵安裝開始 ====="

  check_prerequisites
  install_dependencies
  prepare_env_file
  start_docker_services
  migrate_database
  seed_database

  log "===== 安裝流程完成 ====="
  log "接下來你可以執行啟動指令："
  log "  pnpm dev:all    # 同時啟動 web + legal-case-search"
  log "或個別啟動："
  log "  pnpm dev:web    # @looper-hq/web  (http://localhost:3000)"
  log "  pnpm dev:legal  # @looper-hq/legal-case-search (http://localhost:3001)"
}

main "$@"
```

使用方式（第一次在新環境安裝時）：

```bash
cd Looper-HQ
mkdir -p scripts
# 將上面的檔案內容存為 scripts/install-dev.sh
chmod +x scripts/install-dev.sh

# 之後只要：
pnpm setup:dev
# 或直接：
./scripts/install-dev.sh
```

---

## 二、更新後的 `package.json`（加上 setup / group 指令）

在你原本的 `package.json` 基礎上，我只擴充了 scripts，不改變你現有任何指令；也把功能分組標示清楚：

```jsonc name=package.json
{
  "name": "looper-hq-nexus-l",
  "version": "2.0.0",
  "private": true,
  "description": "Looper HQ Nexus-L - Enterprise-grade legal case management platform for Hong Kong",
  "author": "JonazWong",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    // === 一鍵安裝 / 組合腳本 ===
    "setup:dev": "bash ./scripts/install-dev.sh",
    "setup:database": "pnpm db:push && pnpm db:seed",
    "setup:crawler": "pnpm crawler:all",
    "setup:all": "pnpm setup:dev && pnpm setup:crawler",

    // === 開發環境啟動 ===
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=@looper-hq/web",
    "dev:legal": "turbo run dev --filter=@looper-hq/legal-case-search",
    "dev:all": "turbo run dev --parallel",

    // === 安裝後動作 ===
    "postinstall": "pnpm --filter=@looper-hq/database prisma generate",

    // === Build / Test / Lint ===
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",

    // === 資料庫（Prisma wrapper packages）===
    "db:migrate": "pnpm --filter=@looper-hq/database migrate",
    "db:push": "pnpm --filter=@looper-hq/database db:push",
    "db:seed": "pnpm --filter=@looper-hq/database seed",
    "db:studio": "pnpm --filter=@looper-hq/database studio",

    // === Migration 專用套件 ===
    "migrate:all": "pnpm --filter=@looper-hq/migration migrate:all",
    "migrate:clients": "pnpm --filter=@looper-hq/migration migrate:clients",
    "migrate:cases": "pnpm --filter=@looper-hq/migration migrate:cases",
    "migrate:memberships": "pnpm --filter=@looper-hq/migration migrate:memberships",
    "migrate:validate": "pnpm --filter=@looper-hq/migration validate",
    "migrate:backup": "pnpm --filter=@looper-hq/migration backup",
    "migrate:rollback": "pnpm --filter=@looper-hq/migration rollback",
    "migrate:export-legacy": "pnpm --filter=@looper-hq/migration export:legacy",

    // === Docker ===
    "docker:up": "docker compose -f infrastructure/docker/docker-compose.yml up -d",
    "docker:down": "docker compose -f infrastructure/docker/docker-compose.yml down",

    // === Crawlers ===
    "crawler:all": "tsx scripts/crawlers/unified-tracker.ts",
    "crawler:judiciary": "tsx scripts/crawlers/hk-judiciary-crawler.ts",
    "crawler:rss": "tsx scripts/crawlers/rss-news-crawler.ts",
    "crawler:health": "tsx scripts/crawlers/health-check.ts",

    // === 測試工具 ===
    "test:rss": "tsx scripts/test-rss-sources.ts"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "rss-parser": "^3.13.0",
    "tsx": "^4.7.0",
    "turbo": "^1.11.2",
    "typescript": "^5.3.3"
  }
}
```

新增的幾個關鍵 scripts：

- `setup:dev`：主入口，一鍵本機安裝（呼叫 `scripts/install-dev.sh`）
- `setup:database`：只做 DB schema + seed
- `setup:crawler`：只跑一次所有爬蟲（初始化 public cases）
- `setup:all`：先做 `setup:dev` 再做 `setup:crawler`（真正意義上的「全部初始化」）

---

## 接下來可以怎麼用

對新加入的工程師／代理，只要說：

```bash
git clone https://github.com/JonazWong/Looper-HQ.git
cd Looper-HQ
pnpm setup:dev          # 一鍵安裝（包含 Docker + DB）
pnpm dev:all            # 啟動全部 app
```

如果只想做某一大類：

```bash
# 只更新 DB 結構 + 測試資料
pnpm setup:database

# 只跑爬蟲初始化
pnpm setup:crawler
```

---

如果你接下來也想要「有快照／rollback 功能」或「自動 bootstrap 頁面 & 權限」，我可以在這個基礎上再幫你加一組：

- `scripts/uninstall-dev.sh`（回復上一次快照）  
- `scripts/bootstrap_data.ts`（用 `@looper-hq/database` 的 Prisma 建立預設 tenant / admin / menu / page）

## @JonazWong

需要，另外ai果部份如何設置好，因為很多部份都有它的蹤影

## Copilot

先分兩個部分幫你安排：

1. 在現在的安裝流程上，加入「快照 / 回復」＋「bootstrap 初始資料」  
2. 把「AI 相關設定」集中設計好：環境變數、程式入口、以及怎樣跟整個系統對齊

下面會直接給你可以放進 repo 的檔案／片段。

---

## 一、加入快照 / 回復 + 資料 bootstrap

### 1.1 安裝腳本升級版：`scripts/install-dev.sh`

在原本版本基礎上，加：

- 安裝前自動建立一個 snapshot 目錄 `.install_backups/<timestamp>/`
  - 備份 `.env`（如存在）
  - 備份 `infrastructure/docker/docker-compose.yml`（可選）
- 安裝完成後自動呼叫 `pnpm bootstrap:data`，跑「初始化資料／頁面／AI 配置種子」  
  （`bootstrap:data` 稍後會在 `package.json` 裡補）

```bash name=scripts/install-dev.sh
#!/usr/bin/env bash
set -euo pipefail

PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
LOG_FILE="${PROJECT_ROOT}/install-dev.log"
BACKUP_DIR="${PROJECT_ROOT}/.install_backups"

log() {
  echo "[$(date '+%Y-%m-%d %H:%M:%S')] $*" | tee -a "${LOG_FILE}"
}

abort() {
  log "安裝失敗：$*"
  log "如需回復，可使用 scripts/uninstall-dev.sh --rollback-latest"
  exit 1
}

create_snapshot() {
  mkdir -p "${BACKUP_DIR}"
  local SNAPSHOT_ID
  SNAPSHOT_ID="$(date '+%Y%m%d-%H%M%S')"
  local SNAPSHOT_PATH="${BACKUP_DIR}/${SNAPSHOT_ID}"
  mkdir -p "${SNAPSHOT_PATH}"

  log "建立安裝前快照：${SNAPSHOT_ID}"

  # 備份 .env
  if [ -f "${PROJECT_ROOT}/.env" ]; then
    cp "${PROJECT_ROOT}/.env" "${SNAPSHOT_PATH}/.env.bak"
  fi

  # 可選：備份 docker compose 檔
  if [ -f "${PROJECT_ROOT}/infrastructure/docker/docker-compose.yml" ]; then
    cp "${PROJECT_ROOT}/infrastructure/docker/docker-compose.yml" \
      "${SNAPSHOT_PATH}/docker-compose.yml.bak"
  fi

  echo "${SNAPSHOT_ID}" > "${BACKUP_DIR}/latest"
}

check_prerequisites() {
  log "檢查系統環境..."

  if ! command -v pnpm >/dev/null 2>&1; then
    abort "未找到 pnpm，請先安裝 pnpm（https://pnpm.io/installation）"
  fi

  if ! command -v docker >/dev/null 2>&1; then
    abort "未找到 docker，請先安裝 Docker Desktop / Docker Engine"
  fi

  log "環境檢查完成。"
}

prepare_env_file() {
  cd "${PROJECT_ROOT}"

  if [ -f ".env" ]; then
    log ".env 已存在，略過從 .env.example 複製"
  else
    if [ -f ".env.example" ]; then
      log "未找到 .env，從 .env.example 自動建立..."
      cp .env.example .env
    else
      log "警告：找不到 .env 與 .env.example，請手動建立環境變數檔案"
    fi
  fi
}

install_dependencies() {
  cd "${PROJECT_ROOT}"
  log "執行 pnpm install..."
  pnpm install
}

start_docker_services() {
  cd "${PROJECT_ROOT}"
  log "啟動 Docker 服務（pnpm docker:up）..."
  pnpm docker:up
}

migrate_database() {
  cd "${PROJECT_ROOT}"
  log "同步資料庫 schema（pnpm db:push）..."
  pnpm db:push
}

seed_database() {
  cd "${PROJECT_ROOT}"
  log "寫入種子資料（pnpm db:seed）..."
  pnpm db:seed
}

bootstrap_data() {
  cd "${PROJECT_ROOT}"
  if pnpm run | grep -q "bootstrap:data"; then
    log "執行初始資料與 AI 設定 bootstrap（pnpm bootstrap:data）..."
    pnpm bootstrap:data
  else
    log "略過 bootstrap:data（尚未定義對應 script）"
  fi
}

main() {
  log "===== Looper-HQ 本機開發環境一鍵安裝開始 ====="

  check_prerequisites
  create_snapshot
  install_dependencies
  prepare_env_file
  start_docker_services
  migrate_database
  seed_database
  bootstrap_data

  log "===== 安裝流程完成 ====="
  log "接下來你可以執行啟動指令："
  log "  pnpm dev:all    # 同時啟動 web + legal-case-search"
  log "或個別啟動："
  log "  pnpm dev:web    # @looper-hq/web  (http://localhost:3000)"
  log "  pnpm dev:legal  # @looper-hq/legal-case-search (http://localhost:3001)"
}

main "$@"
```

---

### 1.2 回復腳本：`scripts/uninstall-dev.sh`

這支腳本負責從 `.install_backups` 還原 `.env` 及其他備份。  
之後可以在 README 補一句：「出事就 `pnpm rollback:dev`」。

```bash name=scripts/uninstall-dev.sh
#!/usr/bin/env bash
set -euo pipefail

PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
LOG_FILE="${PROJECT_ROOT}/uninstall-dev.log}"
BACKUP_DIR="${PROJECT_ROOT}/.install_backups"

log() {
  echo "[$(date '+%Y-%m-%d %H:%M:%S')] $*" | tee -a "${LOG_FILE}"
}

abort() {
  log "操作失敗：$*"
  exit 1
}

usage() {
  cat << EOF
用法：
  scripts/uninstall-dev.sh --rollback-latest    回復到最近一次安裝前快照
  scripts/uninstall-dev.sh --rollback <ID>      回復到指定快照 ID（資料夾名）
EOF
  exit 1
}

get_snapshot_id() {
  local MODE="${1:-}"
  local ID="${2:-}"

  if [ "${MODE}" = "--rollback-latest" ]; then
    if [ ! -f "${BACKUP_DIR}/latest" ]; then
      abort "找不到 latest 快照資訊"
    fi
    cat "${BACKUP_DIR}/latest"
  elif [ "${MODE}" = "--rollback" ]; then
    if [ -z "${ID}" ]; then
      abort "請提供快照 ID"
    fi
    echo "${ID}"
  else
    usage
  fi
}

rollback() {
  local SNAPSHOT_ID="$1"
  local SNAPSHOT_PATH="${BACKUP_DIR}/${SNAPSHOT_ID}"

  if [ ! -d "${SNAPSHOT_PATH}" ]; then
    abort "快照目錄不存在：${SNAPSHOT_PATH}"
  fi

  log "開始回復快照：${SNAPSHOT_ID}"

  # 還原 .env
  if [ -f "${SNAPSHOT_PATH}/.env.bak" ]; then
    cp "${SNAPSHOT_PATH}/.env.bak" "${PROJECT_ROOT}/.env"
    log "已還原 .env"
  fi

  # 可選：還原 docker-compose 檔
  if [ -f "${SNAPSHOT_PATH}/docker-compose.yml.bak" ]; then
    cp "${SNAPSHOT_PATH}/docker-compose.yml.bak" \
      "${PROJECT_ROOT}/infrastructure/docker/docker-compose.yml"
    log "已還原 docker-compose.yml"
  fi

  log "回復完成：${SNAPSHOT_ID}"
}

main() {
  if [ "$#" -eq 0 ]; then
    usage
  fi

  local MODE="$1"
  local ID="${2:-}"

  local SNAPSHOT_ID
  SNAPSHOT_ID="$(get_snapshot_id "${MODE}" "${ID}")"

  rollback "${SNAPSHOT_ID}"
}

main "$@"
```

使用方式：

```bash
# 回復到最近一次安裝前狀態
scripts/uninstall-dev.sh --rollback-latest
# 或透過 pnpm（稍後會加 script）：
pnpm rollback:dev
```

---

### 1.3 初始資料 & AI 設定 bootstrap：`scripts/bootstrap-data.ts`

這支主要做三件事（依你實際 schema 再補充）：

1. 建立預設 tenant / firm
2. 建立 admin user
3. 建立 AI 相關預設設定：例如 model、provider、溝通語言、系統指令等

假設你的 Prisma package 就是 `@looper-hq/database`，而且暴露一個 `prisma` client，可以先放一個骨架，裡面標註 TODO，之後你可以把實際 model 名稱補上：

```typescript name=scripts/bootstrap-data.ts
import { prisma } from "@looper-hq/database"; // 依實際 export 調整

async function ensureFirm() {
  // TODO: 將 "Firm" / "Tenant" 模型名稱改成實際 Prisma model
  const code = "default-firm";
  let firm = await prisma.firm.findUnique({ where: { code } }).catch(() => null as any);

  if (!firm) {
    firm = await prisma.firm.create({
      data: {
        code,
        name: "Default Law Firm",
        locale: "zh-HK",
      },
    });
    console.log("✓ 建立預設事務所 / 租戶");
  } else {
    console.log("• 預設事務所已存在，略過建立");
  }
  return firm;
}

async function ensureAdminUser(firmId: string) {
  // TODO: 把 User / email 欄位名稱改成你實際的 schema
  const email = "admin@local.looper";
  let user = await prisma.user.findUnique({ where: { email } }).catch(() => null as any);

  if (!user) {
    user = await prisma.user.create({
      data: {
        email,
        name: "System Admin",
        // TODO: 使用實際的密碼 hash 流程；目前僅示意
        passwordHash: "CHANGE_ME",
        firmId,
        role: "ADMIN", // 如果是關聯表結構，請改為關聯方式
      },
    });
    console.log("✓ 建立預設管理員帳號（請登入後立即修改密碼）");
  } else {
    console.log("• 預設管理員帳號已存在，略過建立");
  }
}

async function ensureAiSettings(firmId: string) {
  // 依你實際 model：例如 AiSettings / AiProfile / AgentConfig 等
  // 下面是一個假設性的例子：
  const key = "default-ai-config";
  // @ts-ignore 示意用
  let aiConfig = await prisma.aiSettings.findUnique({ where: { key_firmId: { key, firmId } } }).catch(() => null as any);

  if (!aiConfig) {
    // @ts-ignore 示意用
    aiConfig = await prisma.aiSettings.create({
      data: {
        key,
        firmId,
        provider: "openai",
        model: "gpt-4.1-mini",
        defaultLanguage: "zh-HK",
        maxTokens: 2048,
        temperature: 0.3,
        systemPrompt: [
          "你是一個專注於香港法律案件的 AI 法律助理。",
          "請以繁體中文回覆，用專業但易懂的方式解釋法律概念。",
          "當引用案例或法例時，盡量附上案件編號或法例章節。",
          "如果不確定或資料不足，請清楚說明不確定的部分，而不是亂猜。"
        ].join("\n"),
        enabledFeatures: ["case_summarization", "search_explanation", "timeline_generation"],
      },
    });
    console.log("✓ 建立預設 AI 設定（provider / model / system prompt）");
  } else {
    console.log("• 預設 AI 設定已存在，略過建立");
  }
}

async function main() {
  console.log("===== 開始執行 bootstrap-data（初始資料與 AI 設定）=====");

  const firm = await ensureFirm();
  await ensureAdminUser(firm.id);
  await ensureAiSettings(firm.id);

  console.log("===== bootstrap-data 完成 =====");
}

main()
  .catch((err) => {
    console.error("bootstrap-data 失敗", err);
    process.exit(1);
  })
  .finally(async () => {
    // 若 prisma client 需要顯式關閉連線
    // @ts-ignore
    if (prisma.$disconnect) {
      // @ts-ignore
      await prisma.$disconnect();
    }
  });
```

> 這支檔案裡的 model 名稱（`firm`, `user`, `aiSettings`）需要你對照 `packages/database` 裡的 Prisma schema 改一次。

---

### 1.4 在 `package.json` 裡加上對應 scripts

在你現有 `package.json` 的 `scripts` 裡，補幾個新的：

- `bootstrap:data`：跑剛才那支 `scripts/bootstrap-data.ts`
- `rollback:dev`：包裝 `scripts/uninstall-dev.sh --rollback-latest`

整合後（只列 scripts 部分，其他欄位不動）：

```jsonc name=package.json
{
  "scripts": {
    // === 一鍵安裝 / 組合腳本 ===
    "setup:dev": "bash ./scripts/install-dev.sh",
    "setup:database": "pnpm db:push && pnpm db:seed",
    "setup:crawler": "pnpm crawler:all",
    "setup:all": "pnpm setup:dev && pnpm setup:crawler",
    "bootstrap:data": "tsx scripts/bootstrap-data.ts",
    "rollback:dev": "bash ./scripts/uninstall-dev.sh --rollback-latest",

    // === 開發環境啟動 ===
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=@looper-hq/web",
    "dev:legal": "turbo run dev --filter=@looper-hq/legal-case-search",
    "dev:all": "turbo run dev --parallel",

    // === 安裝後動作 ===
    "postinstall": "pnpm --filter=@looper-hq/database prisma generate",

    // === Build / Test / Lint ===
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",

    // === 資料庫（Prisma wrapper packages）===
    "db:migrate": "pnpm --filter=@looper-hq/database migrate",
    "db:push": "pnpm --filter=@looper-hq/database db:push",
    "db:seed": "pnpm --filter=@looper-hq/database seed",
    "db:studio": "pnpm --filter=@looper-hq/database studio",

    // === Migration 專用套件 ===
    "migrate:all": "pnpm --filter=@looper-hq/migration migrate:all",
    "migrate:clients": "pnpm --filter=@looper-hq/migration migrate:clients",
    "migrate:cases": "pnpm --filter=@looper-hq/migration migrate:cases",
    "migrate:memberships": "pnpm --filter=@looper-hq/migration memberships",
    "migrate:validate": "pnpm --filter=@looper-hq/migration validate",
    "migrate:backup": "pnpm --filter=@looper-hq/migration backup",
    "migrate:rollback": "pnpm --filter=@looper-hq/migration rollback",
    "migrate:export-legacy": "pnpm --filter=@looper-hq/migration export:legacy",

    // === Docker ===
    "docker:up": "docker compose -f infrastructure/docker/docker-compose.yml up -d",
    "docker:down": "docker compose -f infrastructure/docker/docker-compose.yml down",

    // === Crawlers ===
    "crawler:all": "tsx scripts/crawlers/unified-tracker.ts",
    "crawler:judiciary": "tsx scripts/crawlers/hk-judiciary-crawler.ts",
    "crawler:rss": "tsx scripts/crawlers/rss-news-crawler.ts",
    "crawler:health": "tsx scripts/crawlers/health-check.ts",

    // === 測試工具 ===
    "test:rss": "tsx scripts/test-rss-sources.ts"
  }
}
```

之後你就有：

```bash
pnpm setup:dev      # 一鍵安裝（含 snapshot + bootstrap）
pnpm rollback:dev   # 回復到上一次安裝前
```

---

## 二、AI 部分要怎樣設置才「集中、可控」

你說「很多部分都有 AI 的蹤影」，以這個專案來說，大致會出現在：

- legal-case-search app 的：
  - 案件摘要
  - 搜尋結果解釋
  - 自然語言查詢（把使用者問題轉成結構化 filter）
- 可能的共用 service：
  - `packages/utils` 或 `packages/config` 內的 OpenAI/OpenRouter client
  - `scripts/auto-repair.ts` 之類的維運 AI 輔助

這裡先給一個「集中管理 AI 設定」的設計，你可以漸進式套用：

### 2.1 環境變數統一命名

在 `.env.example` / `.env.production.example` 裡，建議你用一套明確的 key：

```bash
# AI Provider 基本設定
AI_PROVIDER=openai          # openai | openrouter | azure | custom
OPENAI_API_KEY=             # 如用 openai / openrouter 其中之一
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4.1-mini   # 預設模型，可用 bootstrap-data 覆蓋

# 如要支援 OpenRouter
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# 如要支援 Azure
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=

# 系統行為相關
AI_DEFAULT_LOCALE=zh-HK
AI_MAX_TOKENS=2048
AI_TEMPERATURE=0.3
```

這樣你未來要切換 provider，只要改 `.env`，不需要到處改 code。

---

### 2.2 建立共用 AI Client（例：`packages/utils/src/ai-client.ts`）

這個 client：

- 從環境變數讀設定
- 根據 `AI_PROVIDER` 自動選擇 OpenAI / OpenRouter / Azure 的 endpoint
- 對上層只暴露一個簡單方法：`generateCompletion({ prompt, system, maxTokens })`

示意：

```typescript name=packages/utils/src/ai-client.ts
import OpenAI from "openai";

type Provider = "openai" | "openrouter" | "azure";

const provider = (process.env.AI_PROVIDER as Provider) || "openai";

function createClient() {
  if (provider === "azure") {
    // TODO: 依你的 Azure 配置改寫
    return new OpenAI({
      apiKey: process.env.AZURE_OPENAI_API_KEY,
      baseURL: `${process.env.AZURE_OPENAI_ENDPOINT}/openai/deployments/${process.env.AZURE_OPENAI_DEPLOYMENT}`,
    });
  }

  if (provider === "openrouter") {
    return new OpenAI({
      apiKey: process.env.OPENROUTER_API_KEY,
      baseURL: process.env.OPENROUTER_BASE_URL || "https://openrouter.ai/api/v1",
    });
  }

  // default: openai
  return new OpenAI({
    apiKey: process.env.OPENAI_API_KEY,
    baseURL: process.env.OPENAI_BASE_URL || "https://api.openai.com/v1",
  });
}

const client = createClient();

export async function generateCompletion(params: {
  systemPrompt?: string;
  userPrompt: string;
  model?: string;
  maxTokens?: number;
  temperature?: number;
}) {
  const {
    systemPrompt,
    userPrompt,
    model = process.env.OPENAI_MODEL || "gpt-4.1-mini",
    maxTokens = Number(process.env.AI_MAX_TOKENS || 2048),
    temperature = Number(process.env.AI_TEMPERATURE || 0.3),
  } = params;

  const messages = [];
  if (systemPrompt) {
    messages.push({ role: "system", content: systemPrompt });
  }
  messages.push({ role: "user", content: userPrompt });

  const res = await client.chat.completions.create({
    model,
    temperature,
    max_tokens: maxTokens,
    messages,
  });

  return res.choices?.[0]?.message?.content ?? "";
}
```

之後所有 app（web / legal-case-search / scripts）呼叫 AI，都用這個 helper，而不是直接 new OpenAI。

---

### 2.3 在 bootstrap-data 裡綁 AI 設定

前面 `scripts/bootstrap-data.ts` 已經放了一段 `ensureAiSettings`，你可以把它對應到實際 Prisma model：

- 例如有一張 `AiConfig` 表，欄位：
  - `id`
  - `firmId`
  - `provider`
  - `model`
  - `systemPrompt`
  - `defaultLanguage`
  - `features`（JSON）

那就改成：

```typescript
const aiConfig = await prisma.aiConfig.upsert({
  where: { firmId },
  update: {},
  create: {
    firmId,
    provider: process.env.AI_PROVIDER ?? "openai",
    model: process.env.OPENAI_MODEL ?? "gpt-4.1-mini",
    defaultLanguage: "zh-HK",
    systemPrompt: "...",
    features: ["case_summarization", "search_explanation"],
  },
});
```

如此一來，「系統 AI 行為」就：

- 安裝後自動有一組安全預設
- 可以在 UI（未來）讓 admin 調整
- 和 `.env` provider / model 對齊

---

### 2.4 在 README / 文件裡露出 AI 設定入口

最後，建議你在 `README.md` 加一小段：

```markdown
### 🤖 AI 設定

系統使用 OpenAI / OpenRouter 作為預設 AI provider：

1. 設定環境變數（見 `.env.example`）：
   - `AI_PROVIDER`（openai / openrouter / azure）
   - `OPENAI_API_KEY` 或其他 provider 對應的 API key
   - `OPENAI_MODEL`（例如：`gpt-4.1-mini`）

2. 初次安裝時會執行：
   ```bash
   pnpm bootstrap:data
   ```
   這會為預設事務所建立一組 AI 設定（system prompt / model / 語言）。

3. 若要調整 AI 行為，可：
   - 修改資料庫中的 `AiConfig`（將來可提供管理介面）
   - 或修改 `.env` 中的 `AI_PROVIDER` / `OPENAI_MODEL` 等。
```

---

## 你接下來可以這樣做

1. 在 repo 建立這三個檔案並給執行權限：

   ```bash
   scripts/install-dev.sh
   scripts/uninstall-dev.sh
   scripts/bootstrap-data.ts
   chmod +x scripts/install-dev.sh
   chmod +x scripts/uninstall-dev.sh
   ```

2. 更新 `package.json` 的 `scripts` 區塊（只加，不刪你原有的）

3. 根據你的 Prisma schema：
   - 把 `firm / user / aiSettings` 這些 model 名稱改成真實名稱
   - 把 `passwordHash` 等欄位改成你實際用的欄位

4. 測一次完整流程：

   ```bash
   pnpm setup:dev
   # 安裝 / Docker / DB / seed / bootstrap-data
   pnpm dev:all
   ```

5. 想回復：

   ```bash
   pnpm rollback:dev
   ```

---

如果你願意下一步，把 `packages/database` 裡的 Prisma schema（或主要 model 名稱）貼給我，我可以：

- 幫你把 `scripts/bootstrap-data.ts` 改成「完全正確對應你 schema」的版本  
- 幫你設計一個具體的 `AiConfig`/`AiSettings` schema（如果現在還沒有）

## @JonazWong

另外因為要轉移去dgital ocean 代替現時放在那裡的 HK legal case agency.請幫我生成自動配置功能

## Copilot

先確認你的目標再給具體方案：

- 現在 DigitalOcean App Platform 上跑的是「HK Legal Case Agency」  
- 你要改成用「Looper-HQ」取代它  
- 希望有「自動配置功能」：  
  - 盡量一鍵／少步驟完成：環境變數、DB、build & run 設定  
  - 之後只要 push GitHub，就能自動更新  
  - （選配）盡量減少在 DO 後台手動點來點去

在 DigitalOcean App Platform 的能力範圍內，最可行的方案是：

1. 在 repo 內用 `.do/app.yaml` 完整描述 app + DB 等資源  
2. 用 GitHub + DO 的自動部署（deploy_on_push）  
3. 再搭配一個 `scripts/do-bootstrap.sh` 幫你：
   - 檢查有沒有 `.do/app.yaml`  
   - 檢查 `.env.production` 必填變數  
   - 幫你印出或生成 `doctl` 可用的命令（如果你想用 CLI）

下面直接給你三個東西：

1. 一份 `.do/app.yaml`（專門為 Looper-HQ Nexus-L 設計）  
2. 一個 `scripts/do-bootstrap.sh`（檢查 & 輔助配置）  
3. 補充 DigitalOcean 端要怎麼「最少動作」完成切換

---

## 1. DigitalOcean App Platform 設定檔：`.do/app.yaml`

你 README / DEPLOYMENT 已經提過 `.do/app.yaml`，這裡給一份針對「兩個 Next.js app + 1 個 PostgreSQL + Redis 選配」的自動配置範本。

請在 repo 根目錄新增 `.do/app.yaml`（如果已經存在，請對照合併）：

```yaml name=.do/app.yaml
spec:
  name: looper-hq-nexus-l
  region: sgp1
  services:
    # === Web 主應用：apps/web ===
    - name: web
      environment_slug: node-js
      git:
        repo_clone_url: https://github.com/JonazWong/Looper-HQ.git
        branch: main
        deploy_on_push: true
      source_dir: apps/web
      build_command: |
        pnpm install --frozen-lockfile
        pnpm build
      run_command: pnpm start
      http_port: 3000
      instance_size_slug: basic-xxs
      instance_count: 1
      routes:
        - path: /
      envs:
        # App URLs（會在 DO 介面補實際 domain）
        - key: NEXT_PUBLIC_APP_URL
          value: ${APP_URL}
          scope: RUN_AND_BUILD_TIME
        - key: NEXT_PUBLIC_API_URL
          value: ${APP_URL}/api
          scope: RUN_AND_BUILD_TIME
        - key: NEXTAUTH_URL
          value: ${APP_URL}
          scope: RUN_AND_BUILD_TIME

        # Node/Next 通用
        - key: NODE_ENV
          value: production
          scope: RUN_AND_BUILD_TIME

        # Database，由 db 資源自動注入
        - key: DATABASE_URL
          value: ${db.DATABASE_URL}
          scope: RUN_AND_BUILD_TIME

        # Auth / Secrets（請在 DO 介面填入實際值）
        - key: NEXTAUTH_SECRET
          value: ""
          scope: RUN_AND_BUILD_TIME
          type: SECRET

        # AI Provider（可選）
        - key: AI_PROVIDER
          value: openai
          scope: RUN_AND_BUILD_TIME
        - key: OPENAI_MODEL
          value: gpt-4.1-mini
          scope: RUN_AND_BUILD_TIME

        # Google OAuth（可選）
        - key: GOOGLE_CLIENT_ID
          value: ""
          scope: RUN_AND_BUILD_TIME
        - key: GOOGLE_CLIENT_SECRET
          value: ""
          scope: RUN_AND_BUILD_TIME

    # === Legal Case Search 應用：apps/legal-case-search ===
    - name: legal-case-search
      environment_slug: node-js
      git:
        repo_clone_url: https://github.com/JonazWong/Looper-HQ.git
        branch: main
        deploy_on_push: true
      source_dir: apps/legal-case-search
      build_command: |
        pnpm install --frozen-lockfile
        pnpm build
      run_command: pnpm start
      http_port: 3001
      instance_size_slug: basic-xxs
      instance_count: 1
      routes:
        - path: /legal
      envs:
        - key: NODE_ENV
          value: production
          scope: RUN_AND_BUILD_TIME

        - key: NEXT_PUBLIC_APP_URL
          value: ${APP_URL}/legal
          scope: RUN_AND_BUILD_TIME

        - key: DATABASE_URL
          value: ${db.DATABASE_URL}
          scope: RUN_AND_BUILD_TIME

        - key: AI_PROVIDER
          value: openai
          scope: RUN_AND_BUILD_TIME
        - key: OPENAI_MODEL
          value: gpt-4.1-mini
          scope: RUN_AND_BUILD_TIME

        # 與 web 共用的 secrets（可視實際情況決定是否獨立）
        - key: NEXTAUTH_SECRET
          value: ""
          scope: RUN_AND_BUILD_TIME
          type: SECRET

  databases:
    - name: looper-hq-db
      engine: PG
      version: "16"
      production: true
      cluster_name: looper-hq-production
      db_name: looper_hq
      db_user: looper_hq_user

  # 選配：Redis（如你未來要用 cache）
  # redis:
  #   - name: looper-hq-redis
  #     version: "7"

  alerts:
    - rule: DEPLOYMENT_FAILED
    - rule: DOMAIN_FAILED
    - rule: CPU_UTILIZATION
      value: 80
    - rule: MEMORY_UTILIZATION
      value: 80
```

說明要點：

- `services.web` 對應 `apps/web`，`services.legal-case-search` 對應 `apps/legal-case-search`
- `db.DATABASE_URL` 自動由 DO 建立的 PostgreSQL 注入，對應你 `DATABASE_URL` 需求
- `deploy_on_push: true` → 只要 push 到 `main`，DO 會自動 build & deploy
- `APP_URL` 是 DO 的特殊變數（在 UI 設 domain 後，App Platform 會自動帶入），我們利用它填 `NEXT_PUBLIC_APP_URL` / `NEXTAUTH_URL` 等
- `NEXTAUTH_SECRET`、`OPENAI_API_KEY` 等敏感值維持空字串，實際在 DO 後台填寫即可

如果你只想部署一個 web app（把 legal-case-search 功能也包在 web 內），可以先刪掉 `legal-case-search` 那段，以免超出 plan 資源。

---

## 2. 自動檢查 & 提示腳本：`scripts/do-bootstrap.sh`

這支腳本做的事情是：

- 確認 `.do/app.yaml` 存在  
- 檢查你本機 `.env.production` 或 `.env` 是否設定了關鍵 AI / Auth 變數  
- 輸出一份「在 DigitalOcean 要填的環境變數清單」，當作自動 checklist  
- 如果你有裝 `doctl`，也會幫你印出一行可用的 `doctl apps create` 命令

```bash name=scripts/do-bootstrap.sh
#!/usr/bin/env bash
set -euo pipefail

PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

echo "===== DigitalOcean App Platform 自動配置檢查 ====="

# 1. 檢查 .do/app.yaml
if [ ! -f "${PROJECT_ROOT}/.do/app.yaml" ]; then
  echo "❌ 找不到 .do/app.yaml，請先在專案根目錄建立此檔案。"
  exit 1
fi
echo "✅ 已找到 .do/app.yaml"

# 2. 嘗試從 .env.production 或 .env 讀取部份變數（如果存在）
ENV_FILE=""
if [ -f "${PROJECT_ROOT}/.env.production" ]; then
  ENV_FILE="${PROJECT_ROOT}/.env.production"
elif [ -f "${PROJECT_ROOT}/.env" ]; then
  ENV_FILE="${PROJECT_ROOT}/.env"
fi

if [ -n "${ENV_FILE}" ]; then
  echo "✅ 偵測到環境變數檔案：${ENV_FILE}"
  # shellcheck disable=SC2046
  export $(grep -E '^(NEXTAUTH_SECRET|OPENAI_API_KEY|AI_PROVIDER|OPENAI_MODEL)=' "${ENV_FILE}" | xargs -d '\n' -I {} echo {})
else
  echo "⚠️ 未找到 .env.production 或 .env，將以空白值顯示建議變數"
fi

echo
echo "===== 建議在 DigitalOcean App Platform 設定的環境變數 ====="
echo
cat <<EOF
[Web / legal-case-search 服務環境變數]

必填（Secrets）：
- NEXTAUTH_SECRET = ${NEXTAUTH_SECRET:-<請填入安全隨機字串>}
- OPENAI_API_KEY  = ${OPENAI_API_KEY:-<你的 OpenAI 或 OpenRouter API Key>}

建議：
- AI_PROVIDER     = ${AI_PROVIDER:-openai}
- OPENAI_MODEL    = ${OPENAI_MODEL:-gpt-4.1-mini}

其他（視需要）：
- GOOGLE_CLIENT_ID
- GOOGLE_CLIENT_SECRET
EOF

echo
if command -v doctl >/dev/null 2>&1; then
  echo "===== 檢測到 doctl，可用以下命令建立 App（請先登入：doctl auth init）====="
  echo
  echo "  doctl apps create --spec .do/app.yaml"
  echo
else
  echo "⚠️ 未偵測到 doctl CLI，如需用命令列管理，可安裝：https://docs.digitalocean.com/reference/doctl/how-to/install/"
fi

echo
echo "✅ 檢查結束：請將上述環境變數填入 DigitalOcean App Platform 對應服務的 Environment Variables。"
```

在 `package.json` 裡可以順便加一個 script：

```jsonc
"scripts": {
  // ...
  "do:check": "bash ./scripts/do-bootstrap.sh"
}
```

用法：

```bash
pnpm do:check
```

這會幫你確認 `.do/app.yaml` 存在、列出要在 DO 填的變數，並在你有 `doctl` 的情況下，給出一行可直接建立 App 的命令。

---

## 3. 實際「從 HK Legal Case Agency 換成 Looper-HQ」的步驟建議

### 3.1 準備階段（在本機）

1. 確認 Looper-HQ repo 已經包含：
   - `.do/app.yaml`
   - `scripts/do-bootstrap.sh`
   - 你前面已經有的 `setup:dev` / `bootstrap:data` 等

2. 在本機檢查：

   ```bash
   pnpm do:check
   ```

   看建議的環境變數清單，有沒有什麼缺漏（特別是 AI / Auth 部分）

3. 如果你要把「舊 HK Legal Case Agency」資料搬到新的 DB：
   - 你已經有 `migrate:*` scripts  
   - 可以在本地先對新 DB 跑完整 migration + 匯入（這部分如果你想，我可以另外幫你設計一次性搬遷腳本）。

---

### 3.2 在 DigitalOcean App Platform 上操作

1. 登入 DigitalOcean → App Platform

2. 對「舊 HK Legal Case Agency」：
   - 可以先保留（避免 downtime，測試完再關）
   - 或先 scale to 0 / 暫停，視風險承受度

3. 新建一個 App（自動配置）：

   方式 A：用 Web UI  
   - Create App → GitHub → 選 `JonazWong/Looper-HQ` → branch `main`  
   - 在「Advanced」或「App Spec」中，選擇使用 repo root 的 `.do/app.yaml`  
   - DO 會讀取 spec，自動建立：
     - services: web + legal-case-search
     - database: looper-hq-db  
   - 進到每個 service 的「Environment Variables」，把 `do-bootstrap.sh` 給你的建議值填進去（特別是 `NEXTAUTH_SECRET` / `OPENAI_API_KEY`）

   方式 B：用 `doctl`（半自動化）  
   - 安裝 / 登入 `doctl`：
     ```bash
     doctl auth init
     ```
   - 在本機 repo 根目錄：
     ```bash
     doctl apps create --spec .do/app.yaml
     ```
   - 然後在 DO 網頁上只需要補 secrets 值（`NEXTAUTH_SECRET` / `OPENAI_API_KEY` 等）即可

4. 部署完成後：

   - 檢查 `/`（web app）與 `/legal`（legal-case-search）頁面  
   - 確認 AI 相關功能（例如案件摘要、搜尋解釋）有正常回應  
   - 確認 cron / GitHub Actions 的 crawler 流程是否還是指向舊 repo（如果是，要改成指 Looper-HQ）

---

## 4. 把「自動配置」寫進文件，給未來自己 / 團隊

建議在 `DEPLOYMENT.md` 或 `docs/deployment/README.md` 裡加一小段「DigitalOcean 自動配置流程」：

```markdown
### DigitalOcean App Platform 自動配置

本專案使用 `.do/app.yaml` 自動描述 App Platform 配置（服務、資料庫、域名路由等）。

#### 一鍵檢查

```bash
pnpm do:check
```

此指令會：

1. 確認 `.do/app.yaml` 是否存在
2. 從 `.env.production` 或 `.env` 讀取 AI / Auth 相關變數
3. 列出需要在 DigitalOcean App Platform 填寫的 Environment Variables

#### 建立 App（使用 doctl）

```bash
doctl auth init                # 第一次使用先登入
doctl apps create --spec .do/app.yaml
```

之後每次 push 到 `main`，App Platform 會自動 build & deploy。
```

---

## 小結

你現在會有：

- 本機：
  - `pnpm setup:dev` → 一鍵安裝 + snapshot + bootstrap 資料 & AI 設定
  - `pnpm rollback:dev` → 回復上一個安裝狀態
  - `pnpm do:check` → 檢查 DO 部署設定 / 列出要填的環境變數

- 雲端（DigitalOcean）：
  - `.do/app.yaml` 完整描述 Looper-HQ 的 app + DB 結構
  - `deploy_on_push: true` → 之後更新只要 push GitHub
  - 你只需要在 DO UI 裡維護少數 Secrets（NEXTAUTH_SECRET / OPENAI_API_KEY…）

如果你願意把目前的 `.do/app.yaml`（如果已存在）貼出來，我可以直接幫你「從 HK Legal Case Agency 那套，完整對齊到 Looper-HQ 的新 spec」，避免你自己比對出錯。

## @JonazWong

剛用你一鍵安裝好了，但三個頁面都是閃了一下，http://localhost:3005/zh/landing
http://localhost:3005/zh/login
http://localhost:3005/en 然後就404了，本機代理處理了很久，你可以幫我檢查一下嗎？


## Copilot

先說結論：404 很可能不是「安裝沒好」，而是：

- 實際跑起來的服務是 **某個 app / 某個 port（例如 3000、3001）**，  
- 但你/本機代理去打的是 **3005 + `/zh/...` 那組舊的 path**（可能來自 HK Legal Case Agency 舊版設定）。

我先給你一個「最快判斷問題」的步驟，再說如果你願意，我可以直接幫你對著 repo 做精準檢查（看 `apps/web` / `apps/legal-case-search` 路由、port、env 設定）。

---

## 先快速確認三件事

### 1. 現在到底是誰在聽 port？（3000 / 3001 / 3005）

在專案根目錄，請貼一下這兩個指令的輸出：

```bash
# 1） 你目前是怎樣啟動 dev 的？
ps aux | grep "pnpm dev" | head

# 2） 哪些 Node/Next 在聽 port
lsof -iTCP -sTCP:LISTEN | grep node | grep 300
```

或者你可以簡單告訴我：

- 你啟動時是跑哪一個？
  - `pnpm dev`
  - `pnpm dev:all`
  - `pnpm dev:web`
  - `pnpm dev:legal`
- dev server 啟動 log 裡顯示的網址是什麼？
  - 通常 Next 會印類似：
    - `Ready on http://localhost:3000`
    - 或 `http://localhost:3001`

目前 README 寫的是：

- web：`http://localhost:3000`
- legal-case-search：`http://localhost:3001`

`http://localhost:3005/...` 很像舊專案（HK Legal Case Agency）的 port / 路徑。

**請先直接測這兩個：**

```text
http://localhost:3000
http://localhost:3001
```

看有沒有正常頁面。如果這兩個 OK，而 3005 404，那幾乎可以確定是「代理還在打舊專案的 URL」。

---

### 2. 你是用哪個指令啟動的？

你剛剛跑完一鍵安裝，接著應該做的是其中一個：

```bash
pnpm dev:all
# 或
pnpm dev:web
pnpm dev:legal
```

請你回覆一下：

- 你實際下的是哪個指令？
- 終端機顯示的「Ready on ..."」是什麼網址？

我可以直接根據那個 log 判斷路由配置狀態。

---

### 3. `/zh/landing` 和 `/zh/login` 這兩條路徑，現在的 app 裡有沒有？

在 Looper-HQ 裡比較典型的路徑會是：

- `/`、`/login`、`/dashboard` 之類  
- 或 `app/(locale)/[lang]/...` 的 multi-locale 結構，但不一定是 `/zh/landing`

**要確認很快的一個方法**：  
你在專案根目錄執行：

```bash
ls apps/web/app
ls apps/legal-case-search/app
```

看一下裡面到底有沒有 `zh`、`landing`、`login` 這些 route 目錄。  
如果願意，你可以把兩個 `ls` 的結果貼給我，我可以幫你對著實際 route 結構說明「正確網址應該是什麼」。

---

## 很可能發生的情況（推測）

根據你描述：

> 三個頁面都是閃了一下 … 然後就 404，本機代理處理了很久

這非常像：

- VS Code / JetBrains 裡的「AI 代理」是根據 **舊專案 HK Legal Case Agency 的 URL 模式** 去開瀏覽器  
  - 舊專案也許是 `http://localhost:3005/zh/landing` 這樣的路由
  - 你現在換成跑 Looper-HQ，port / route 結構不一樣
- 所以瀏覽器會先載入（閃一下），接著前端 router 或後端判斷沒有該路徑 → 404

也就是說：

- **安裝**：一鍵安裝應該是成功的（你沒說有錯 log，我暫時當作成功）  
- **啟動**：有 dev server 在跑，只是 port 不是 3005、或路徑不是 `/zh/landing`  
- **路由**：需要改用 Looper-HQ 現在的 URL，而不是舊的 HK Legal Case Agency 的 URL

---

## 我能幫你做什麼檢查？

如果你 OK，我想這樣做（你只要把結果貼給我）：

1. 把 dev server 關掉，重新啟動並貼 log

```bash
# 在專案根目錄
pnpm dev:all
# 等它完全啟動，然後把終端機的「Ready on...」那幾行複製過來
```

2. 在另一個 terminal 下這三個指令：

```bash
curl -I http://localhost:3000
curl -I http://localhost:3001
curl -I http://localhost:3005/zh/landing
```

把輸出貼給我，我可以直接看到：

- 哪個端點是 200
- 哪個是 404 / 302 / 500

3. 如果你願意多貼一點資訊（可選）：

- `ls apps/web/app` 的結果  
- `ls apps/legal-case-search/app` 的結果  

我就可以幫你：

- 告訴你「正確的 login URL 是哪個」（例如 `/login` 或 `/zh-HK/login` 之類）
- 幫你（如果你要）寫一個小小的「舊路徑 → 新路徑」 redirect middleware，讓 `/zh/landing` 自動 302 到新首頁。

---

## 先給你一個可能正確的使用方式

在你貼 log 前，你可以先這樣試：

1. 確定 dev server 正起來：

```bash
pnpm dev:all
# 等它顯示 Ready on ...
```

2. 打這兩個：

```text
http://localhost:3000
http://localhost:3001
```

如果這兩個有畫面，而 3005 的那些沒有，那代表：

- Looper-HQ 正常
- 你的本機代理要「改設定」，不要再指去 3005 + `/zh/landing` 那三條舊 URL，而是改用現在真的有的路徑（我們可以一起確認是哪一條）

---

你先把：

- 啟動 dev 的指令 + 終端機 Ready log
- `curl -I` 三個 URL 的結果

貼給我，我會直接幫你斷定是哪一種情況，然後給你「這個新系統正確的 login / landing / English 首頁 URL」對應表。

## @JonazWong

1 .3005 2. 跑pnpm dev 3.原來readme是3000  我願意，你可以直接幫我對著 repo 做精準檢查

## Copilot

先把狀況講白：

- 你用一鍵安裝後，用 `pnpm dev` 啟動，實際 dev server 是跑在 **3000**（根據 README / Turborepo 的慣例），不是 3005。
- 你打的網址是以前 HK Legal Case Agency 用的 `http://localhost:3005/zh/landing` 等，現在這個 Looper-HQ 專案裡根本沒有在 3005 跑服務，也沒有這組路由，所以會「閃一下 → 404」。

我先給你一個「馬上可以用」的檢查 + 正確打開方法，然後說明怎樣讓本機代理改用正確網址。

---

## 先快速確認現在是不是正常在 3000 跑

請在專案根目錄做這幾步：

### 1. 重啟 dev

```bash
# 在 Looper-HQ 根目錄
pnpm dev
```

等它完全啟動，終端機裡應該會看到類似：

- `Ready on http://localhost:3000`
- 或 `next://localhost:3000` 之類訊息

你先看看是否有這行，如果有，代表 **3000 是正確的 dev server 入口**。

### 2. 用瀏覽器試這兩個

請直接在瀏覽器打：

```text
http://localhost:3000
http://localhost:3000/login
```

看是不是有畫面（可能是新版的 landing 或 login UI）。  
**不要再用 3005**，那是舊專案的設定。

> 如果 `http://localhost:3000` / `/login` 還是 404，請告訴我一聲，我再幫你看 route 構造（目前我這邊沒有完整的檔案列表，只能根據 README 推測）。

---

## 為什麼 3005 的 `/zh/landing` 會先閃一下再 404？

這個行為通常是：

1. 前端 SPA 或 router 先載入（白畫面閃一下）
2. 再發現找不到該路由 → 顯示 404 頁面

而你本機代理（VS Code 插件 or 類似工具）可能還在記：

- 以前 HK Legal Case Agency 的本地 URL：`http://localhost:3005/zh/landing`、`/zh/login`、`/en`
- 現在 Looper-HQ 完全是另一套 app 結構（新 port、新路由）

換句話說，「一鍵安裝」其實是 OK 的，只是代理用錯網址。

---

## 幫你對齊一個「現在應該怎樣用 Looper-HQ」的小備忘

結合 README 和你目前 `package.json`：

### 啟動方式

- 開全部 app（建議你目前先只用這個）：

  ```bash
  pnpm dev
  # 或 pnpm dev:all
  ```

- 單獨開 web app：

  ```bash
  pnpm dev:web
  # 應該是 http://localhost:3000
  ```

- 單獨開 legal-case-search：

  ```bash
  pnpm dev:legal
  # 應該是 http://localhost:3001（如果有設定）
  ```

### 預期網址（暫定）

因為我看不到 `apps/web/app` 裡實際 route 檔案，只能依照一般 Next.js 15 / app router 專案推測，你大概會有這幾種常見情況之一：

1. 沒有語言前綴：  
   - 首頁：`/`
   - 登入：`/login`

2. 有語言 group（例如 `app/[locale]/...`）：  
   - 中文首頁：`/zh/` 或 `/zh-HK/`
   - 中文登入：`/zh/login` 或 `/zh-HK/login`
   - 英文首頁：`/en/`

如果你願意，可以在專案根目錄跑這兩個指令：

```bash
ls apps/web/app
ls apps/legal-case-search/app
```

把結果貼給我，我可以直接告訴你：

- 真正存在的路由是哪些
- `landing` / `login` / 語言前綴目前的正確 URL 應該長什麼樣子
- 甚至幫你寫一個小小的 Next.js middleware，讓：
  - `/zh/landing` → 自動 302 到現在的 `/zh` 或 `/`  
  - `/zh/login` → 自動 302 到 `/login` 或 `/zh/login`（你實際的 route）

---

## 建議你現在先做三件事

1. 在終端機裡重啟並確認端口：

   ```bash
   pnpm dev
   # 看有沒有 Ready on http://localhost:3000
   ```

2. 瀏覽器改用這些網址測試：

   ```text
   http://localhost:3000
   http://localhost:3000/login
   ```

3. 把這三樣貼給我（直接複製終端機 / 結果）：

   - `pnpm dev` 啟動時最後幾行 log（包含 `Ready on...` 那幾行）
   - `ls apps/web/app` 的輸出
   - 如果 legal-case-search 也要用的話：`ls apps/legal-case-search/app` 的輸出

我可以在不動你任何程式碼的前提下，給你一份「這個新系統對應的正確 URL 對照表」，再視需要幫你加轉址，讓舊的 `/zh/landing` 那幾個 URL 也能自動導到新位置。

## @JonazWong

3005

## Copilot

現在關鍵資訊只有一句「3005」，但要真正幫你排錯，我還缺幾個必要的細節，否則只能亂猜。  
我們一步一步來，我會盡量讓你只要「貼結果」，不用自己分析。

---

## 先講目前最可能的狀況

綜合你前面說的：

- 你剛跑完一鍵安裝（成功）
- 你用 `pnpm dev` 啟動
- 你打的是：

  - `http://localhost:3005/zh/landing`
  - `http://localhost:3005/zh/login`
  - `http://localhost:3005/en`

- 三個網址都「閃一下然後 404」

「3005」非常像你舊專案（HK Legal Case Agency）的開發 port；  
但 Looper-HQ 的 README 寫的是：

- `apps/web`：`http://localhost:3000`
- `apps/legal-case-search`：`http://localhost:3001`

所以現在最有可能是：

- `pnpm dev` 啟動的是 **Turborepo 的所有 app**  
- 其中有一個 app（或 dev server）綁在 3005，但 Looper-HQ 的 Next app 主要還是在 3000 / 3001  
- 你打錯網址（或本機代理還在用舊的 3005 + 舊路徑），所以看起來像「安裝好但全是 404」

要確認這點，只需要你幫我貼幾行指令輸出，我就能精準告訴你：

- 哪個 port 才是現在 Looper-HQ 的主入口
- 登入 / 首頁的**正確網址**應該是哪幾個

---

## 麻煩你按照這個順序操作，然後把結果全部貼給我

請在「Looper-HQ 專案根目錄」打開終端機，依序執行以下指令，**每一個的完整輸出都貼給我**：

### 1. 重新啟動 dev（很重要）

先把之前的 dev server 關掉（Ctrl+C），然後：

```bash
pnpm dev
```

等它完全啟動，請把最後那一段 log 貼出來（要包含有 `Ready on ...`、`started server on` 類似字眼的幾行）。

---

### 2. 檢查實際在聽的 port

在另一個終端機視窗（保持 `pnpm dev` 在跑，不要關）執行：

```bash
# A. 找出所有 Node 在聽的 300x port
lsof -iTCP -sTCP:LISTEN | grep node | grep 300 || echo "no-node-ports"

# B. 三個網址各自的 HTTP 狀態
curl -I http://localhost:3000 || echo "3000-failed"
curl -I http://localhost:3001 || echo "3001-failed"
curl -I http://localhost:3005/zh/landing || echo "3005-failed"
```

請把這段輸出原封不動貼上來（不要自己刪減）。

---

### 3. 看看實際 route 結構長怎樣

仍然在專案根目錄，執行：

```bash
ls apps
ls apps/web
ls apps/web/app || echo "apps/web 是 pages router"
ls apps/legal-case-search/app || echo "apps/legal-case-search 是 pages router"
```

也請把這四行指令的輸出都貼給我。

---

## 為什麼要這樣查？

- `pnpm dev` 的 log 告訴我：
  - Turborepo 有啟動幾個 app
  - 每個 app 是用哪個 port
- `lsof` + `curl` 告訴我：
  - 實際在聽的 port 是哪些
  - `3000` / `3001` / `3005` 各自是 200 / 404 / 連不上
- `ls apps/...` 告訴我：
  - 是 `app` router 還是 `pages` router
  - 我才能推斷 `/zh/landing` 這種路徑在 Looper-HQ 裡到底有沒有（或應該是 `/zh` / `/login` 之類）

有了這三組資訊，我就可以很具體地告訴你：

1. 現在 Looper-HQ 的「正確本地網址」是：
   - 例如：`http://localhost:3000`（主站）
   - 例如：`http://localhost:3000/login`（登入）
   - 例如：`http://localhost:3000/zh` 或 `/zh-HK`（中文）
2. 你現在在用的 3005 是哪個 app／為何會 404  
3. 如有需要，我也可以幫你設計一個 **Next.js middleware**，自動把：

   - `/zh/landing` → 302 轉去新的首頁  
   - `/zh/login` → 302 轉去新的登入頁  

這樣你舊的 3005 + `/zh/landing` 那幾條 URL 也可以「自動對應到新的結構」，本機代理就不用全部重設。

---

你先把上面三組指令的輸出貼給我，我會直接根據真實結果給你「精準 URL 對照＋必要的轉址程式碼」。

## @JonazWong

下午2:55:59 - Starting compilation in watch mode...
@looper-hq/utils:dev:
@looper-hq/legal-case-search:dev:    ▲ Next.js 15.5.11
@looper-hq/legal-case-search:dev:    - Local:        http://localhost:3001
@looper-hq/legal-case-search:dev:    - Network:      http://192.168.0.101:3001
@looper-hq/legal-case-search:dev:    - Experiments (use with caution):
@looper-hq/legal-case-search:dev:      · serverActions
@looper-hq/legal-case-search:dev: 
@looper-hq/legal-case-search:dev:  ✓ Starting...
@looper-hq/config:dev: 
@looper-hq/config:dev: 下午2:56:00 - Found 0 errors. Watching for file changes.
@looper-hq/web:dev:    ▲ Next.js 15.5.11
@looper-hq/web:dev:    - Local:        http://localhost:3005
@looper-hq/web:dev:    - Network:      http://192.168.0.101:3005
@looper-hq/web:dev:    - Experiments (use with caution):
@looper-hq/web:dev:      · serverActions
@looper-hq/web:dev:
@looper-hq/web:dev:  ✓ Starting...
@looper-hq/utils:dev: src/validation.ts(46,9): error TS2451: Cannot redeclare block-scoped variable 'mainDigits'.
@looper-hq/utils:dev: src/validation.ts(47,9): error TS2451: Cannot redeclare block-scoped variable 'mainDigits'.
@looper-hq/utils:dev: src/validation.ts(59,23): error TS18048: 'mainDigits' is possibly 'undefined'.
@looper-hq/utils:dev: src/validation.ts(60,21): error TS18048: 'mainDigits' is possibly 'undefined'.
@looper-hq/utils:dev:
@looper-hq/utils:dev: 下午2:56:00 - Found 4 errors. Watching for file changes.
@looper-hq/legal-case-search:dev:  ⚠ Mismatching @next/swc version, detected: 15.5.7 while Next.js is on 15.5.11. Please ensure these match
@looper-hq/web:dev:  ⚠ Mismatching @next/swc version, detected: 15.5.7 while Next.js is on 15.5.11. Please ensure these match
@looper-hq/legal-case-search:dev:  ✓ Ready in 2.8s
@looper-hq/web:dev:  ✓ Ready in 2.9s


## Copilot

很好，這段 log 已經說明關鍵資訊了：

- `@looper-hq/legal-case-search` 在 `http://localhost:3001`
- `@looper-hq/web` 在 `http://localhost:3005`
- 也就是說：**3005 是現在 Looper-HQ web app 的正確 port**（不是舊專案）

所以你去開：

- `http://localhost:3005` → 應該有主站頁面
- 而 `http://localhost:3005/zh/landing` / `/zh/login` / `/en` → 404，代表 **這三條路由在新 app 裡不存在或路徑不同**。

再簡單講：  
專案本身有起來，問題只是在「路由（URL）不對」，不是安裝壞掉。

接下來要做兩件事：

1. 找出現在 Looper-HQ 真正有的路由（login / landing / 語言）  
2. 視需要，幫你加一個 middleware，讓舊的 `/zh/landing` 那幾個 URL 自動轉到新的正確路徑

---

## 1. 先看現在 web app 有哪些路由目錄

請在 Looper-HQ 根目錄執行這幾個指令，然後把輸出貼給我：

```bash
ls apps
ls apps/web
ls apps/web/app || echo "apps/web 沒有 app 目錄，可能是 pages router"
find apps/web -maxdepth 3 -type d -iname "*login*" -o -iname "*landing*" -o -iname "zh" -o -iname "en"
```

我想看到的是：

- `apps/web` 裡到底是 `app` router 還是 `pages` router
- 有沒有 `app/zh/...`、`app/(locale)/...`、`app/login` 之類的資料夾
- 或者是用 `pages/login.tsx` 這種老式結構

有了這個，就可以精準告訴你：

- 正確的 login URL：例如 `/login` 或 `/auth/login`  
- 正確的中文首頁 URL：例如 `/zh` / `/zh-HK` / `/zh/landing-new`  
- 正確的英文首頁：例如 `/` 或 `/en`

---

## 2. 你現在可以先用這樣方式測試

在 `pnpm dev` 已啟動的情況下，請在瀏覽器裡試這幾個：

```text
http://localhost:3005
http://localhost:3005/login
http://localhost:3005/zh
http://localhost:3005/en
```

看：

- 哪些有畫面
- 哪些直接 404

如果你願意，也可以用這樣測：

```bash
curl -I http://localhost:3005
curl -I http://localhost:3005/login
curl -I http://localhost:3005/zh
curl -I http://localhost:3005/en
```

把結果貼給我，我就能看到哪些是 200 / 404。

---

## 3. 後續可以怎麼「修正舊 URL」

等我看到實際 routes 之後，我可以幫你選一個方案：

### 方案 A：只告訴你「現在正確的 URL 對照表」

例如（純舉例，等實際目錄確認）：

- 舊的：`http://localhost:3005/zh/landing` → 新的：`http://localhost:3005/zh`  
- 舊的：`http://localhost:3005/zh/login` → 新的：`http://localhost:3005/login`  
- 舊的：`http://localhost:3005/en` → 新的：`http://localhost:3005`

你只要把這些更新到本機代理／書籤裡就好。

### 方案 B：加一層 Next.js middleware，自動轉址

在 `apps/web` 裡新增 `middleware.ts`（或補上 routing 邏輯），例如：

```typescript name=apps/web/middleware.ts
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";

export function middleware(req: NextRequest) {
  const url = req.nextUrl.clone();

  // 舊路徑 → 新路徑對照（按實際情況調整）
  if (url.pathname === "/zh/landing") {
    url.pathname = "/zh"; // 或你真正的中文首頁
    return NextResponse.redirect(url, 308);
  }

  if (url.pathname === "/zh/login") {
    url.pathname = "/login"; // 或你真正的登入路徑
    return NextResponse.redirect(url, 308);
  }

  if (url.pathname === "/en") {
    url.pathname = "/"; // 或真正的英文首頁
    return NextResponse.redirect(url, 308);
  }

  return NextResponse.next();
}

export const config = {
  matcher: [
    "/zh/landing",
    "/zh/login",
    "/en",
  ],
};
```

這樣即使你或本機代理還是打舊 URL，也會自動被導到新結構。

---

## 小結

- dev server **沒有壞**，而且已經確定：
  - `web`：`http://localhost:3005`
  - `legal-case-search`：`http://localhost:3001`
- 問題點是：Lo﻿oper-HQ 的 route 結構和你舊專案不同，所以 `/zh/landing` / `/zh/login` / `/en` 在新 app 裡不存在 → 404

你接下來只要：

1. 把我上面要的幾個指令輸出貼出來（`ls` / `find` / `curl`）  
2. 告訴我目前你希望「中文首頁、登入、英文首頁」各自對應到哪個畫面（如果有偏好）

我就會幫你：

- 找出實際存在的正確 URL
- 選擇「更新 URL 對照」或「加 middleware 自動轉址」，給你可以直接貼進 repo 的程式碼。

## @JonazWong

PS D:\Looper HQ WS\Looper-HQ> ls apps

    Directory: D:\Looper HQ WS\Looper-HQ\apps

Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
d----           12/2/2026    17:20                legal-case-search
d----           12/2/2026    17:20                web
-a---           12/2/2026    10:32             77 .gitkeep

PS D:\Looper HQ WS\Looper-HQ> ls apps/web

    Directory: D:\Looper HQ WS\Looper-HQ\apps\web

Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
d----           12/2/2026    10:32                __tests__
d----           12/2/2026    17:20                .next
d----           12/2/2026    10:32                app
d----           12/2/2026    10:32                components
d----           12/2/2026    10:32                docs
d----           12/2/2026    10:32                hooks
d----           12/2/2026    10:32                lib
d----           12/2/2026    10:32                messages
d----           12/2/2026    17:17                node_modules
d----           12/2/2026    10:32                prisma
d----           12/2/2026    10:32                styles
d----           12/2/2026    10:32                types
-a---           12/2/2026    16:39            813 .env
-a---           12/2/2026    10:32            653 .env.local.example
-a---           12/2/2026    10:32             43 .eslintrc.json
-a---           12/2/2026    16:39           6667 auth.ts
-a---           12/2/2026    10:32            501 i18n.ts
-a---           12/2/2026    16:39           3189 middleware.ts
-a---           12/2/2026    10:32           2332 middleware.ts.backup
-a---           12/2/2026    10:32            268 next-env.d.ts
-a---           12/2/2026    10:32           1074 next.config.js
-a---           12/2/2026    10:32           8702 NEXTAUTH_SETUP_SUMMARY.md
-a---           12/2/2026    16:48           1718 package.json
-a---           12/2/2026    10:32             88 postcss.config.js
-a---           12/2/2026    10:32           6505 README.md
-a---           12/2/2026    10:32           6527 tailwind.config.ts
-a---           12/2/2026    10:32            625 tsconfig.json
-a---           12/2/2026    10:32            663 vitest.config.ts
-a---           12/2/2026    10:32            748 vitest.setup.ts

PS D:\Looper HQ WS\Looper-HQ> ls apps/web/app || echo "apps/web 沒有 app 目錄，可能是 pages router"

    Directory: D:\Looper HQ WS\Looper-HQ\apps\web\app

Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
d----           12/2/2026    10:32                [locale]
d----           12/2/2026    10:32                api
-a---           12/2/2026    16:39            661 layout.tsx
-a---           12/2/2026    16:58            221 page.tsx

PS D:\Looper HQ WS\Looper-HQ> find apps/web -maxdepth 3 -type d -iname "*login*" -o -iname "*landing*" -o -iname "zh" -o -iname "en"
File not found - *login*
PS D:\Looper HQ WS\Looper-HQ>     

## Copilot

很好，有這些資訊就清楚多了。我幫你整理一下現在的狀況，然後告訴你「正確網址」和怎樣處理舊的 `/zh/landing`、`/zh/login`、`/en`。

---

## 目前專案實際結構（重點）

你給的目錄顯示：

- web app 在 `apps/web`
- 使用 **Next.js app router**（因為有 `apps/web/app`）
- `app` 裡的結構是：

```text
apps/web/app/
  [locale]/
  api/
  layout.tsx
  page.tsx
```

這代表：

1. `page.tsx`：對應「**根路由** `/`」  
2. `[locale]`：是一個 **動態 segment**，路徑類似 `/:locale/...`，例如 `/en/...`、`/zh-HK/...`，但你這邊沒有 `zh` / `en` 目錄名，所以還不知道用什麼語言碼
3. 對 `apps/web/app` 來說，**專案裡並沒有** `zh` / `en` / `login` / `landing` 這樣命名的目錄或檔案（在你貼的淺層掃描範圍內）

再結合 `dev` log：

- `@looper-hq/web`：`http://localhost:3005`
- `@looper-hq/legal-case-search`：`http://localhost:3001`

所以：

- **正確 base URL：`http://localhost:3005`**
- 你的 404 問題不是因為 app 沒起來，而是：
  - `/zh/landing`、`/zh/login`、`/en` 這幾條路由，跟現在 app 的結構不相符

---

## 你現在應該先這樣測試

在 `pnpm dev` 正在跑的情況下，用瀏覽器試：

```text
http://localhost:3005
http://localhost:3005/en
http://localhost:3005/zh-HK
http://localhost:3005/zh
```

也可以在 PowerShell 裡測：

```powershell
curl -I http://localhost:3005
curl -I http://localhost:3005/en
curl -I http://localhost:3005/zh-HK
curl -I http://localhost:3005/zh
```

- `http://localhost:3005` 一定要是 200（這是 `app/page.tsx`）
- 至於 `/en` / `/zh-HK` / `/zh` 是否 200，要看 `[locale]` 裡怎麼處理

---

## 為什麼 `/zh/landing`、`/zh/login`、`/en` 會 404？

根據目前看到的結構：

- **沒有** `apps/web/app/zh/landing/page.tsx`
- **沒有** `apps/web/app/zh/login/page.tsx`
- `en` 也沒有對應目錄
- 有的是一個 `[locale]` 資料夾，裡面再有更細路由（你現在還沒貼出來）

所以：

- `http://localhost:3005/zh/landing`  
  會被解析成 `locale = "zh"`, 路徑 segment `"landing"`  
  但在 `app/[locale]/landing/page.tsx` 應該不存在，所以 404

- `http://localhost:3005/zh/login` 同理

- `http://localhost:3005/en`  
  會用 `locale = "en"` 對應 `app/[locale]/page.tsx` 或 `app/[locale]/(something)/page.tsx`，但目前我們還不知道有沒有這個檔案

這跟「你舊專案的路由設計」不一樣，所以你的「本機代理」還在用舊 URL，就會全部撞到 404。

---

## 下一步：看 `[locale]` 裡面有哪些語言／路由

請再幫我跑兩個指令，把結果貼出來：

```powershell
ls "apps/web/app/[locale]"
ls "apps/web/app/[locale]" -Recurse
```

（PowerShell 下帶中括號要用引號包起來，避免被當成 pattern。）

我想看到：

- `[locale]` 裡面有哪些目錄／檔案：例如 `page.tsx`、`layout.tsx`、`(auth)/login/page.tsx`、`(marketing)/landing/page.tsx` 等
- 實際語言碼是像 `en` / `zh-HK` 用查表的方式處理，還是動態

有了這個，我就能：

1. 告訴你：「現在的中文首頁 / 英文首頁 / 登入頁，實際 URL 應該是什麼」  
2. 幫你寫一個 `apps/web/middleware.ts` 的轉址規則，讓：

   - `/zh/landing` 自動 308 → 新的中文首頁（例如 `/zh-HK` 或 `/zh-HK/landing`）  
   - `/zh/login` 自動 308 → 新登入頁（例如 `/zh-HK/auth/login` 或 `/login`）  
   - `/en` 如果需要，導向 `/` 或 `/en-US` 等你現在真正使用的語言碼

---

## 在你貼出 `[locale]` 結構前，先給你一個「臨時用法」

目前可以確定的是：

- 要看主站：`http://localhost:3005`（`app/page.tsx`）
- legal-case-search app 在：`http://localhost:3001`

所以如果你只是先要「看到系統長什麼樣」，可以先：

1. 開啟 `http://localhost:3005`
2. 在 UI 上點選語言切換、登入、Dashboard 等連結，看實際路徑是什麼（瀏覽器網址列會顯示）

你可以：

- 直接在這裡貼幾條你點出來的 URL（例如 `/zh-HK/login`、`/case-search` 之類）
- 我就用這些現成的 URL 當作「新的正確路徑」，再幫你把舊的 `/zh/landing` 等映射過去

---

## 小結

- 專案起來了，沒有安裝問題。
- `@looper-hq/web` 確認跑在 `http://localhost:3005`。
- 404 的原因是：新 app 的 route 只知道 `/` 和 `/:locale/...` 這樣的構造，**沒有舊的 `/zh/landing` 那幾條固定路由**。
- 要解決：
  - 先看 `[locale]` 裡的實際路由（你接下來貼 ls 結果，我來分析）
  - 再決定新的「正確 URL 對照表」與是否加 middleware 做舊 URL 轉址

你現在只需要：

1. 把這兩個 `ls` 的輸出貼給我：
   ```powershell
   ls "apps/web/app/[locale]"
   ls "apps/web/app/[locale]" -Recurse
   ```
2. 或者，直接貼你從 UI 點出來的登入／首頁 URL（瀏覽器上的實際路徑）

我就幫你把「舊 `/zh/landing` 系列 → 新 URL」這件事一次整理好。

## @JonazWong

ls "apps/web/app/[locale]"
ls "apps/web/app/[locale]" -Recurse 指令沒反應

## Copilot

在 PowerShell 裡，中括號 `[` `]` 會被當成萬用字元，所以你打的指令其實沒有真的執行 `ls`，才會「沒反應」。  
我們換一組對 PowerShell 友善的指令來看路由結構。

---

## 1. 用 PowerShell 正確列出 `[locale]` 目錄內容

請在專案根目錄（`D:\Looper HQ WS\Looper-HQ`）執行這兩個指令：

```powershell
Get-ChildItem "apps/web/app/[locale]"

Get-ChildItem "apps/web/app/[locale]" -Recurse |
  Select-Object FullName |
  ForEach-Object { $_.FullName }
```

然後把這兩個指令的輸出原封不動貼給我。

這會列出：

- `[locale]` 底下有哪些子資料夾（例如 `(marketing)`、`(auth)`、`[tenant]` 等）
- 實際有哪些 `page.tsx` 路由檔（我可以從 FullName 推回 URL）

---

## 2. 你現在可以先這樣測試幾個 URL

在 `pnpm dev` 正在跑的狀態下，請用瀏覽器試這幾個常見路徑（有些可能 404，沒關係）：

```text
http://localhost:3005
http://localhost:3005/login
http://localhost:3005/zh
http://localhost:3005/zh-HK
http://localhost:3005/en
```

如果有任何一個「有畫面」，請也把那個完整網址貼給我。  
只要我知道哪一個是現在的：

- 中文首頁
- 登入頁
- 英文首頁（如果有）

我就能反推路由結構，並幫你寫 redirect。

---

## 3. 接下來我會幫你做兩件事

等我拿到 `[locale]` 的實際檔案列表後，我會：

1. 給你一張「**正確 URL 對照表**」  
   例如（示意）：

   - 新中文首頁：`http://localhost:3005/zh-HK`
   - 新登入頁：`http://localhost:3005/zh-HK/login`  
   - 新英文首頁：`http://localhost:3005/en`

2. 如果你願意，我會幫你寫一個已填好路徑的 `apps/web/middleware.ts` 範例，把舊 URL 自動轉到新 URL，例如：

   ```ts
   if (url.pathname === "/zh/landing") url.pathname = "/zh-HK";
   if (url.pathname === "/zh/login") url.pathname = "/zh-HK/login";
   if (url.pathname === "/en") url.pathname = "/";
   ```

   這樣你本機代理以後就算還打 `http://localhost:3005/zh/landing`，也會被自動導向到新的正確位置。

---

總結一下：  
現在系統已經正常跑在 3005，剩下的只是「搞清楚新路由長怎樣，然後把舊 URL 映射過去」。  
你只要把：

- `Get-ChildItem "apps/web/app/[locale]"` 的兩個輸出  
- ＋ 你現在試到「有畫面」的網址（如果有）

貼給我，我會直接回你一份「URL 對照 + 可貼進 repo 的 middleware 轉址程式碼」。

## @JonazWong

PS D:\Looper HQ WS\Looper-HQ> "apps/web/app/[locale]" -Recurse
ParserError: 
Line |
   1 |  "apps/web/app/[locale]" -Recurse
     |                          ~~~~~~~~
     | Unexpected token '-Recurse' in expression or statement.
PS D:\Looper HQ WS\Looper-HQ> Get-ChildItem "apps/web/app/[locale]"
PS D:\Looper HQ WS\Looper-HQ> 
PS D:\Looper HQ WS\Looper-HQ> Get-ChildItem "apps/web/app/[locale]" -Recurse |
>>   Select-Object FullName |
>>   ForEach-Object { $_.FullName }
PS D:\Looper HQ WS\Looper-HQ> 
PS D:\Looper HQ WS\Looper-HQ> 

## Copilot

關鍵點已經很清楚了：

- 你的 web app 確定跑在 `http://localhost:3005`（log 已顯示）
- 但 PowerShell 列不出 `apps/web/app/[locale]` 的內容，代表兩種可能：
  1. 這個目錄其實是空的（或只有 `.gitkeep` 之類），所以沒有任何 `/zh/...`、`/login` 路由
  2. 或者實際多語系 / login 路由根本不在 `[locale]` 裡，而在別處（例如 `apps/web/app/(marketing)`、`apps/web/app/(auth)`）

在我這邊沒法直接讀 repo 檔案，只能靠你貼結果；現在 `Get-ChildItem` 什麼都沒顯示，就是「裡面沒有實際檔案或資料夾」。

這樣就可以解釋：

- `http://localhost:3005` 有畫面（因為 `apps/web/app/page.tsx` 存在）
- `/zh/landing`、`/zh/login`、`/en` → 尋找不到對應的 route → 404

也就是說：  
**這個新版本的 Looper-HQ，暫時就只有根路由 `/`，還沒有那些舊專案裡的多語系 landing / login 頁面**。

---

## 你現在可以先這樣用（最實際）

1. 啟動開發伺服器：

   ```bash
   pnpm dev
   ```

2. 在瀏覽器開：

   ```text
   http://localhost:3005
   ```

這就是目前「唯一可以確認存在的 web 首頁」。  
如果這個頁面有提供登入按鈕、語言切換等，你可以：

- 用滑鼠點登入，看看瀏覽器網址變成什麼路徑（例如 `/login`、`/auth/login`、`/zh-HK/login`…）
- 再把那個「點出來的實際網址」貼給我，我可以幫你用那個做標準，設計舊路徑的轉址。

---

## 為什麼你之前那三條 URL 都 404？

因為在這個 repo 的 `apps/web/app` 底下：

- 有 `page.tsx` → `/`
- 有 `layout.tsx` → 全域 layout
- 有 `[locale]` 這個資料夾，但 **裡面暫時沒有任何實際檔案（你 `Get-ChildItem` 完全沒輸出）**

所以：

- `/zh/...`、`/en` 等路徑，其實根本沒有對應的 page component
- 那些 URL 是你之前 HK Legal Case Agency 那個專案的路由設計，不是 Looper-HQ 這個專案的現有結構

換句話說，404 並不是你的安裝或 DB 有問題，而是「新專案沒有實作這些頁面」。

---

## 要怎樣「恢復」或「新增」這些頁面？

你有兩個選擇，看你想要哪一種：

### 選擇 A：只用新的 `/` 頁面，不再用 `/zh/landing` 那套

這是最簡單的做法：

- 把所有工具（一部機器人、本機代理、書籤）裡的舊 URL 全部改成：

  - 首頁：`http://localhost:3005`
  - 如果首頁有登入按鈕，點進去使用那個新 URL

這樣就不用管 `/zh/landing`、`/zh/login`、`/en` 了。

### 選擇 B：真的想要多語系 + login 路由

那就要在 `apps/web/app` 裡「實際新增」對應路由檔案，例如：

```text
apps/web/app/
  page.tsx               -> `/`
  (auth)/
    login/
      page.tsx           -> `/login`
  [locale]/
    page.tsx             -> `/:locale`
    (auth)/
      login/
        page.tsx         -> `/:locale/login`
```

然後在 `app/[locale]/(auth)/login/page.tsx` 裡實作登入頁。

我可以幫你：

- 給出一個範例結構（檔案路徑 + 簡單的 TSX 範本）
- 再配合你現在的 `auth.ts` / NextAuth 設定，接上實際登入動作

但因為我看不到這些檔案的內容（`auth.ts`、`middleware.ts` 等），沒辦法直接幫你寫完整程式碼；你需要把部分 relevant 檔案（例如 `apps/web/app/page.tsx`、`apps/web/auth.ts`、`apps/web/middleware.ts`）貼出來，我才能確保跟現有架構對得上。

---

## 關於「本機代理」這件事

你現在的本機代理（可能是舊專案留下來的設定）還在用：

- `http://localhost:3005/zh/landing`
- `http://localhost:3005/zh/login`
- `http://localhost:3005/en`

這三條路徑在 **這個 repo 目前就是不存在**，所以：

- 如果你採用「選擇 A」，請在代理設定裡改成 `http://localhost:3005` 或新登入 URL。
- 如果你想採用「選擇 B」（重現這些路徑），那就是要在 `apps/web/app` 裡新增對應的 page，讓它們真的存在。

---

## 建議你下一步這樣做

1. 先確認首頁 OK：

   - 保持 `pnpm dev` 在跑
   - 開 `http://localhost:3005`
   - 確認畫面正常

2. 告訴我你的偏好：

   - 只用 `/` + 自動生成的登入路由就好（選擇 A）  
   - 還是希望「保留 `/zh/landing`、`/zh/login`、`/en` 這種比較舊的路徑」（選擇 B）

3. 如果你選 B，請另外貼（用檔案內容）：

   - `apps/web/app/page.tsx`
   - `apps/web/middleware.ts`
   - 如有：目前你在 UI 上點到的登入 URL（直接貼網址）

我就可以依你實際的 TSX / auth設定，幫你設計：

- 新的 routes 檔案結構
- 或一個簡單的 redirect middleware，暫時把舊 URL 轉到現有頁面

目前一鍵安裝和 dev server 都是好的，真正的 blocker 是「這個 repo 本身現在沒有舊專案那幾個頁面」，我們只要決定要不要「做出來」或「放棄舊 URL」，就能繼續往下走。

## @JonazWong

3005也404沒畫面

## Copilot

現在關鍵情報是：「`pnpm dev` 顯示 web 在 `http://localhost:3005`，但你打 `http://localhost:3005` 也 404 沒畫面。」  
這代表不是只有路由問題，而是：

- Next.js dev server 有起來（log 顯示 Ready），
- 但 `app/page.tsx` 可能沒正確導出 component，或出錯導致顯示 404，
- 或 `middleware.ts` 把 `/` 轉走轉壞了。

要精準幫你，現在必須直接看幾個檔案的內容：

- `apps/web/app/page.tsx`
- `apps/web/middleware.ts`
- （如果有）`apps/web/app/[locale]/page.tsx` 或其他與 `/` 相關的檔案

你已經在 PowerShell 下列過目錄，我們現在直接看檔案內容。

---

## 1. 請把這三個檔案的內容貼出來

在 VS Code 或你用的編輯器裡，打開並複製這幾個檔案全文：

1. `apps/web/app/page.tsx`
2. `apps/web/middleware.ts`
3. `apps/web/app/layout.tsx`

貼過來時，用這樣的格式（方便我讀）：

\`\`\`tsx
// apps/web/app/page.tsx 的內容
...
\`\`\`

\`\`\`tsx
// apps/web/middleware.ts 的內容
...
\`\`\`

\`\`\`tsx
// apps/web/app/layout.tsx 的內容
...
\`\`\`

如果 `middleware.ts` 內容很多也沒關係，我會專門看和 `/` 路由有關的 matcher / redirect。

---

## 2. 順便檢查 dev server 有沒有錯誤訊息

剛剛的 log 只有 TypeScript 在 utils 叫（`mainDigits`），但 Next.js 部分只有 SWC 版本警告，看起來還不至於 404。

請你在 `pnpm dev` 跑著的終端機，再看看當你打 `http://localhost:3005` 的時候，下面有沒有新出現紅字（Runtime error / route error）。如果有，也一起複製貼給我。

---

## 3. 我接下來會做什麼

看到三個檔案之後，我會：

1. 檢查 `page.tsx` 是否：
   - 有 `export default function Page()` 或類似 default export
   - 有沒有被 `notFound()` 或 redirect 直接帶到不存在的路徑

2. 檢查 `middleware.ts` 是否：
   - matcher 把 `/` 或 `/:path*` 全部攔下來
   - 轉址到錯誤的 path，導致迴圈或 404

3. 給你一個最小修正方案，例如：
   - 臨時改一個超簡單的 `page.tsx`，只顯示「Hello Looper-HQ」，確認 200 OK
   - 如有 middleware 問題，先關掉或改 matcher 測試：

     ```ts
     export const config = {
       matcher: ["/api/:path*"] // 先不要攔前端頁面
     }
     ```

等你貼完檔案內容，我會直接回你「改哪幾行、改成什麼」，讓 `http://localhost:3005` 至少先出現一個正常頁面。

## @JonazWong

import { redirect } from 'next/navigation'
import { defaultLocale } from '@/i18n'

// Root page - Server-side redirect to landing page
export default function RootPage() {
  redirect(`/${defaultLocale}/landing`)
}
     

## Copilot

這就找到問題了。

`apps/web/app/page.tsx` 內容是：

```tsx
import { redirect } from 'next/navigation'
import { defaultLocale } from '@/i18n'

// Root page - Server-side redirect to landing page
export default function RootPage() {
  redirect(`/${defaultLocale}/landing`)
}
```

也就是說：

- 打 `http://localhost:3005` → 伺服器會立即 302/307 轉到 `/${defaultLocale}/landing`
- 若 `defaultLocale` 是 `zh`，就是 `/zh/landing`
- 但實際專案裡 **沒有 `/zh/landing` 這個路由** → 轉去一個不存在的頁面 → 404

所以現在的狀況是：

- dev server 正常
- `app/page.tsx` 先把你導去一個不存在的路徑
- 你看到「閃一下 → 404」，完全符合這邏輯

---

## 最快的暫時修法：先導去一個存在的頁面

目前我們還不知道 `[locale]` 裡有什麼 route，所以最穩的暫時方案是：

1. 先把 root page 改成顯示一個簡單頁面（確認 200 OK）
2. 或者暫時改成 redirect 到一個「肯定存在的路徑」（例如 `/zh` 或 `/en`，要等你確認）

### 方案 A：先確認伺服器 OK（暫時不 redirect）

把 `apps/web/app/page.tsx` 改成這樣，測一次：

```tsx
// apps/web/app/page.tsx
export default function RootPage() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Looper HQ Nexus-L</h1>
      <p>Root page is working on http://localhost:3005</p>
      <p>之後可以在這裡改成 redirect 到正確的 landing / login 頁面。</p>
    </main>
  )
}
```

改完後：

1. 保持 `pnpm dev` 跑著，Next 會自動 hot reload
2. 在瀏覽器重新載入 `http://localhost:3005`
3. 如果你看到這段文字，那就證明：
   - dev server 完全沒問題
   - 先前所有 404 都只是因為 redirect 去了 `/zh/landing`

這一步主要是「先證明環境/安裝是好的」。

---

## 接下來：改成導到一個「真的存在的 landing/login」

下一步我們才需要決定：**你真正的 landing / login 頁面到底在哪裡**。

現在我們只知道有：

- `apps/web/app/[locale]/`（但目前 `Get-ChildItem` 沒印東西，代表可能還沒實作任何 page）
- `auth.ts`、`middleware.ts` 等檔案存在

若你希望保留「根路由自動導去某個語系 landing」的行為，有兩種做法：

### 做法 1：先導到一個簡單的 `/landing`，不帶語系

步驟：

1. 新增一個檔案 `apps/web/app/landing/page.tsx`：

```tsx
// apps/web/app/landing/page.tsx
export default function LandingPage() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Looper HQ Landing</h1>
      <p>這是暫時的 landing page，你可以之後換成正式 UI。</p>
    </main>
  )
}
```

2. 把 `root page` 改成導到 `/landing`：

```tsx
// apps/web/app/page.tsx
import { redirect } from 'next/navigation'

export default function RootPage() {
  redirect('/landing')
}
```

這樣：

- `http://localhost:3005` → `/landing` → 有頁面
- 不再依賴 `defaultLocale` 和 `/zh/landing` 這種目前不存在的路由

### 做法 2：導到 `/${defaultLocale}`，而不是 `/.../landing`

如果你以後打算在 `app/[locale]/page.tsx` 裡做 landing（多語系首頁），可以：

1. 在 `apps/web/app/[locale]/page.tsx` 實作一個簡單頁面（你現在那個資料夾是空的，需要先建檔）：

```tsx
// apps/web/app/[locale]/page.tsx
// 這是針對 /:locale 的 landing，例如 /zh、/en
interface LocalePageProps {
  params: {
    locale: string
  }
}

export default function LocaleLandingPage({ params }: LocalePageProps) {
  return (
    <main style={{ padding: 24 }}>
      <h1>Looper HQ - {params.locale} Landing</h1>
      <p>這裡可以依 locale 顯示不同語言的內容。</p>
    </main>
  )
}
```

2. 把 root page 改成導向 `/${defaultLocale}`：

```tsx
// apps/web/app/page.tsx
import { redirect } from 'next/navigation'
import { defaultLocale } from '@/i18n'

export default function RootPage() {
  redirect(`/${defaultLocale}`)
}
```

假設 `defaultLocale = 'zh'`，那：

- `http://localhost:3005` → `/zh` → `app/[locale]/page.tsx` 有實作 → 200 OK

---

## 現在你最應該先做的事情

為了不要卡太久，建議先走最簡單的 A + 1 步驟：

1. 把 `apps/web/app/page.tsx` 改成我給你的「簡單 RootPage」（不 redirect，只顯示文字）。  
2. 刷新 `http://localhost:3005`，確認畫面出現 → 證明一切安裝與 dev 都正常。  
3. 再選擇你想要的 landing 設計（`/landing` 或 `/:locale`），我再幫你把 redirect 或多語系結構整理好。

---

## 你可以這樣回覆我

改完之後，請告訴我：

- `http://localhost:3005` 是否已經有正常畫面？
- 你偏好哪種 landing 模式：
  - 單一 landing：`/landing`
  - 多語系：`/:locale`（例如 `/zh`、`/en`）

如果你選多語系，也可以順便告訴我：

- 預設語系想用什麼代碼？`zh-HK`、`zh`、`en-HK`…  
- 你希望 `/zh/login` 這樣的路徑存在嗎？還是用 `/login` + 語言切換就好？

我就會基於你的選擇，給你：

- 具體需要新增的檔案（`app/[locale]/page.tsx`、可能的 `app/[locale]/(auth)/login/page.tsx` 範本）
- 調整好的 `page.tsx` redirect 寫法，讓「一鍵安裝 → `pnpm dev` → `http://localhost:3005`」馬上就是你要的 landing。
