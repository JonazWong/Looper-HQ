# 專案功能配置詳解

> **注意**：本文件依據現時（2026年3月）repo 的實際程式碼與功能所撰寫，僅描述已實際存在的功能。

---

## 目錄

1. [Apps / Services / Packages 清單](#apps--services--packages-清單)
2. [主要頁面（Routes）清單](#主要頁面routes清單)
3. [Landing Page 功能說明](#landing-page-功能說明)
4. [Login / Register 功能說明](#login--register-功能說明)
5. [Dashboard 功能說明](#dashboard-功能說明)
6. [爬蟲與 AI 分析流程](#爬蟲與-ai-分析流程)
7. [i18n / 中英文切換](#i18n--中英文切換)
8. [重要環境變數](#重要環境變數)
9. [常用指令（pnpm scripts）](#常用指令pnpm-scripts)

---

## Apps / Services / Packages 清單

### Apps（Next.js 應用程式）

| 應用 | 目錄 | Port | 用途 |
|------|------|------|------|
| **Web（主應用）** | `apps/web` | 3005 | 主要 Dashboard、案件管理、客戶管理、帳單、時間追蹤等完整功能 |
| **Legal Case Search** | `apps/legal-case-search` | 3001 | 公開法律案件搜尋門戶（輕量版，供公眾使用，不需登入） |

### Packages（共用函式庫）

| 套件 | 目錄 | 用途 |
|------|------|------|
| **database** | `packages/database` | Prisma Client 與 Schema，所有應用共用同一資料庫 |
| **utils** | `packages/utils` | 通用工具函式，如 `getLocalizedField()`（取雙語欄位）|
| **types** | `packages/types` | TypeScript 型別定義（共用型別） |
| **config** | `packages/config` | 設定預設值（ESLint、TypeScript 等）|
| **migration** | `packages/migration` | 資料遷移腳本（從舊版資料庫遷移） |

### Services

目前 `services/` 目錄為空，預留給未來微服務使用。

---

## 主要頁面（Routes）清單

所有路由均在 `apps/web/app/[locale]/` 下，URL 格式為 `/{locale}/{path}`（例如 `/zh/dashboard`、`/en/cases`）。

### 公開路由（不需登入）

| 路徑 | 頁面 | 用途 |
|------|------|------|
| `/{locale}` | `app/[locale]/page.tsx` | 根路由，重定向至 `/landing` |
| `/{locale}/landing` | `app/[locale]/landing/page.tsx` | 主頁，介紹平台功能、登入入口 |
| `/{locale}/login` | `app/[locale]/(auth)/login/page.tsx` | 登入頁面 |
| `/{locale}/register` | `app/[locale]/(auth)/register/page.tsx` | 註冊頁面 |
| `/{locale}/case-search` | `app/[locale]/case-search/page.tsx` | 公開法律案件搜尋（支援全文搜尋） |
| `/{locale}/sitemap` | `app/[locale]/sitemap/page.tsx` | 網站地圖 |

### 需要登入的路由（Dashboard 區域）

| 路徑 | 頁面 | 用途 | 備注 |
|------|------|------|------|
| `/{locale}/dashboard` | `(dashboard)/dashboard/page.tsx` | 主儀表板：統計卡片、快速操作、最近案件、活動記錄 | 所有角色 |
| `/{locale}/cases` | `(dashboard)/cases/page.tsx` | 案件列表：搜尋、篩選、分頁 | 所有角色 |
| `/{locale}/cases/new` | `(dashboard)/cases/new/page.tsx` | 新增案件表單 | ADMIN / LAWYER |
| `/{locale}/cases/[id]` | `(dashboard)/cases/[id]/page.tsx` | 案件詳情：案件資料、文件、時間記錄 | 所有角色 |
| `/{locale}/clients` | `(dashboard)/clients/page.tsx` | 客戶列表與管理 | 所有角色 |
| `/{locale}/documents` | `(dashboard)/documents/page.tsx` | 文件管理：上傳、搜尋、分類 | 所有角色 |
| `/{locale}/billing` | `(dashboard)/billing/page.tsx` | 帳單與發票管理 | ADMIN / LAWYER |
| `/{locale}/calendar` | `(dashboard)/calendar/page.tsx` | 日曆（開庭日期、約會）— 基礎版 | 所有角色 |
| `/{locale}/time-tracking` | `(dashboard)/time-tracking/page.tsx` | 計費時間追蹤 | ADMIN / LAWYER |
| `/{locale}/search` | `(dashboard)/search/page.tsx` | 公開案件搜尋（登入版，含進階篩選） | 所有角色 |
| `/{locale}/services` | `(dashboard)/services/page.tsx` | 會員資料庫 / Member Portal（統計、搜尋入口） | 所有角色 |
| `/{locale}/settings` | `(dashboard)/settings/page.tsx` | 帳戶設定 | 所有角色 |
| `/{locale}/public-cases` | `(dashboard)/public-cases/page.tsx` | 公開案件瀏覽（Dashboard 內）| 所有角色 |
| `/{locale}/admin` | `(dashboard)/admin/page.tsx` | 管理員面板：爬蟲監控、RSS 來源管理 | **ADMIN 限定** |
| `/{locale}/admin/ai-classify` | `(dashboard)/admin/ai-classify/page.tsx` | AI 分類工具 | **ADMIN 限定** |
| `/{locale}/test-case-linking` | `(dashboard)/test-case-linking/page.tsx` | 測試用：案件關聯功能測試頁 | 開發測試用 |

---

## Landing Page 功能說明

**頁面**：`apps/web/app/[locale]/landing/page.tsx`

### 功能
- **語言切換**：右上角有 `LanguageSwitcher` 下拉式選單，可切換中文/英文
- **登入 / 立即開始**：導航至 `/{locale}/login` 及 `/{locale}/register`
- **Hero 區塊**：平台介紹標題、副標題、CTA 按鈕（立即開始 → Register；搜尋案件 → `/case-search`）
- **功能特色卡片**（6 張）：
  - ✅ 可點擊（有連結）：
    - **案件管理** → `/{locale}/cases`
    - **客戶門戶** → `/{locale}/clients`
    - **公開案件搜尋** → `/{locale}/case-search`
    - **時間追蹤** → `/{locale}/time-tracking`
    - **文件管理** → `/{locale}/documents`
  - ℹ️ 純資訊顯示（無連結，`cursor-default`）：
    - **安全合規**（無專屬頁面，僅作說明）
- **CTA 區塊**：再次引導至 Register 頁面
- **Footer**：版權、服務條款、私隱政策文字（靜態，無外部連結）

---

## Login / Register 功能說明

### Login（`/{locale}/login`）
- 使用 **NextAuth.js v5** Credentials Provider（電郵 + 密碼）
- 可選：Keycloak SSO（當 `KEYCLOAK_CLIENT_ID` 及 `KEYCLOAK_ISSUER` 設定後啟用）
- 登入後重定向至 `/{locale}/dashboard`

### Register（`/{locale}/register`）
- 提供電郵、密碼、姓名欄位
- 新建用戶預設角色為 `CLIENT`

---

## Dashboard 功能說明

**頁面**：`apps/web/app/[locale]/(dashboard)/dashboard/page.tsx`  
**Client Component**：`apps/web/components/dashboard/dashboard-content.tsx`

### 統計卡片（StatCard）
| 卡片 | 數據來源 |
|------|---------|
| Total Cases | `prisma.case.count()` |
| Active Clients | `prisma.client.count()` |
| Pending Reviews | `prisma.case.count({ where: { status: 'PENDING' } })` |
| Success Rate | 已完成 / 非取消案件 × 100% |

### 快速操作（Quick Actions）
所有按鈕均有連結：
- New Case → `/{locale}/cases/new`
- Add Client → `/{locale}/clients`
- Search Cases → `/{locale}/search`
- View Cases → `/{locale}/cases`
- View Clients → `/{locale}/clients`
- Upload Documents → `/{locale}/documents`

### 其他元件
- **Case Distribution**（ProgressRing）：案件狀態分布圓環圖
- **Recent Activity**（ActivityTimeline）：最近活動記錄，來自 `prisma.activity`
- **Recent Cases**（列表）：最近 5 個案件，每行可點擊進入案件詳情 `/{locale}/cases/[id]`
- **Premier Exclusive Search**（`PremierSearchCard`）：僅 PREMIUM / PREMIER 會員可見

---

## 爬蟲與 AI 分析流程

### 架構概覽

```
GitHub Actions (daily-case-tracking.yml)
    ↓ pnpm crawler:all
scripts/crawlers/unified-tracker.ts
    ↓ trackRssNews()
scripts/crawlers/rss-news-crawler.ts
    ↓ RssNewsAdapter
apps/web/lib/services/data-sources/rss-news-adapter.ts
    ↓ prisma.publicCase.upsert()
PostgreSQL (public_cases table)
    ↓
scripts/crawlers/unified-tracker.ts
    ↓ prisma.crawlerJobRun.update()
PostgreSQL (crawler_job_runs table)  ← 可觀測性記錄
```

### GitHub Actions 排程

| 工作流程 | 檔案 | 排程 | 功能 |
|---------|------|------|------|
| Daily Case Tracking | `.github/workflows/daily-case-tracking.yml` | 每天 04:00 HKT（20:00 UTC）| 執行 `pnpm crawler:all` |
| RSS Crawler | `.github/workflows/rss-crawler.yml` | 另有獨立排程 | RSS 專用觸發 |

### 資料模型

#### `PublicCase`（`public_cases` 表）
- 儲存從 RSS / 香港司法機構爬取的公開案件
- 去重策略：`@@unique([source, externalId])`
- 重要欄位：`source`、`externalId`、`title_zh`、`title_en`、`keywords`、`crawledAt`

#### `RssSource`（`rss_sources` 表）
- 管理 RSS 來源的設定（URL、fetchInterval、狀態等）
- 爬蟲會根據 `lastFetchAt` 和 `fetchInterval` 決定是否需要重新抓取
- 目前支援的來源（`CaseSource` enum）：
  - `HK_JUDICIARY`（暫時停用，待新 API）
  - `MINGPAO_PNS_RSS`
  - `MINGPAO_INS_RSS`
  - `HKLII`
  - `HKGOVNEWS_RSS`

#### `CrawlerJobRun`（`crawler_job_runs` 表）
- 每次執行 `unified-tracker.ts` 時建立一筆記錄
- 欄位：`status`（RUNNING / SUCCESS / PARTIAL_SUCCESS / FAILED）、`totalAdded`、`totalErrors`、`durationSeconds`、`sourceStats`（JSON）、`startedAt`、`completedAt`
- **在 Admin Dashboard 可查詢最近的執行記錄**

### AI 分析流程

- **入口**：`apps/web/app/api/classify/route.ts` 及 `apps/web/app/[locale]/(dashboard)/admin/ai-classify/page.tsx`
- **AI 提供商**：OpenAI / OpenRouter（由 `OPENAI_API_KEY` 和 `OPENAI_BASE_URL` 控制）
- **功能**：對 `PublicCase` 進行自動分類（案件類別、關鍵字提取）
- **觸發**：目前主要為手動觸發（Admin AI Classify 頁面）

### 如何驗證爬蟲與 AI 分析是否成功

1. **Admin Dashboard**（`/{locale}/admin`）：
   - 查看「爬蟲任務記錄」區塊，可見最近 5 次執行的狀態、新增筆數、錯誤數
   - 查看「新聞源管理」表格的「最後爬取」時間
2. **API 查詢**：`GET /api/stats/crawler`（需 ADMIN 角色）
3. **資料庫直查**：
   ```sql
   SELECT * FROM crawler_job_runs ORDER BY started_at DESC LIMIT 10;
   SELECT COUNT(*), DATE(crawled_at) FROM public_cases GROUP BY DATE(crawled_at) ORDER BY 2 DESC;
   ```
4. **日誌**：GitHub Actions 的 workflow run logs 有詳細的爬蟲執行輸出

---

## i18n / 中英文切換

### 技術棧

- **框架**：`next-intl` v4（整合 Next.js App Router）
- **語言**：繁體中文（`zh`，預設）、英文（`en`）
- **URL 策略**：`/{locale}/path`（locale prefix 永遠存在）
- **訊息檔**：`apps/web/messages/zh.json`、`apps/web/messages/en.json`

### 切換按鈕

`LanguageSwitcher` 元件（`apps/web/components/language-switcher.tsx`）：
- 使用下拉式選單（DropdownMenu）
- 顯示位置：Dashboard Header（`components/layout/header.tsx`）及 Landing Page 頭部
- 切換方式：替換 URL 路徑中的 locale segment（`/zh/...` ↔ `/en/...`）

### 在 Server Component 使用翻譯

```typescript
import { getTranslations } from 'next-intl/server'

const t = await getTranslations({ locale, namespace: 'dashboard' })
return <h1>{t('title')}</h1>
```

### 在 Client Component 使用翻譯

```typescript
import { useTranslations, useLocale } from 'next-intl'

const t = useTranslations('dashboard')
const locale = useLocale()
```

### 新增翻譯

1. 在 `apps/web/messages/zh.json` 和 `apps/web/messages/en.json` 加入對應 key-value
2. 遵循 namespace 結構（例如 `dashboard.title`、`nav.cases`）
3. 雙語資料欄位使用 `_zh`/`_en` 後綴（如 `title_zh`、`title_en`），由 `getLocalizedField()` 函式自動根據 locale 取值

---

## 重要環境變數

從 `.env.example` 整理：

| 環境變數 | 必要 | 說明 |
|---------|------|------|
| `DATABASE_URL` | ✅ 必要 | PostgreSQL 連線字串 |
| `NEXTAUTH_SECRET` | ✅ 必要 | NextAuth.js 加密金鑰（`openssl rand -base64 32`）|
| `NEXTAUTH_URL` | ✅ 必要 | Web 應用 URL（開發：`http://localhost:3005`）|
| `KEYCLOAK_CLIENT_ID` | 可選 | Keycloak OAuth Client ID（不設則使用 Credentials）|
| `KEYCLOAK_ISSUER` | 可選 | Keycloak Realm URL |
| `OPENAI_API_KEY` | 可選 | AI 分析功能所需（可用 OpenRouter key）|
| `OPENAI_MODEL` | 可選 | 預設 `gpt-5.1` |
| `OPENAI_BASE_URL` | 可選 | 預設 OpenAI，可設為 OpenRouter proxy |
| `CRAWLER_ENABLED` | 可選 | 爬蟲開關（`true`/`false`）|
| `RSS_TIMEOUT` | 可選 | RSS 請求逾時（毫秒，預設 30000）|

---

## 常用指令（pnpm scripts）

### 開發

```bash
pnpm dev              # 啟動所有 apps（Turborepo）
pnpm dev:web          # 只啟動 web app（Port 3005）
pnpm dev:legal        # 只啟動 legal-case-search app（Port 3001）
pnpm dev:all          # 平行啟動所有 apps
```

### 資料庫

```bash
pnpm db:push          # 同步 Prisma Schema 到資料庫（開發用，不建立 migration）
pnpm db:migrate       # 建立並套用 migration（生產用）
pnpm db:seed          # 填入測試資料
pnpm db:seed:minimal  # 填入最少量測試資料
pnpm db:studio        # 開啟 Prisma Studio GUI（Port 5555）
```

### Docker

```bash
pnpm docker:up        # 啟動 PostgreSQL、Redis、Keycloak
pnpm docker:down      # 停止所有 Docker 服務
```

### 爬蟲

```bash
pnpm crawler:all      # 執行完整爬蟲（RSS + 司法機構）
pnpm crawler:rss      # 只執行 RSS 爬蟲
pnpm crawler:health   # 檢查爬蟲健康狀態
pnpm crawler:check    # 查詢爬蟲資料統計
```

### 建置與測試

```bash
pnpm build            # 建置所有 apps
pnpm lint             # ESLint 檢查
pnpm test             # 執行測試（Vitest）
pnpm --filter=@looper-hq/web type-check  # TypeScript 型別檢查
pnpm --filter=@looper-hq/database prisma generate  # 重新生成 Prisma Client
```

### 首次設定流程

```bash
pnpm install --frozen-lockfile   # 1. 安裝依賴（自動執行 prisma generate）
cp .env.example .env             # 2. 複製環境設定
pnpm docker:up                   # 3. 啟動 Docker 服務
sleep 15                         # 4. 等待 PostgreSQL 就緒
pnpm db:push                     # 5. 同步資料庫 Schema
pnpm db:seed                     # 6. 填入測試資料（可選）
pnpm dev                         # 7. 啟動開發伺服器
```
